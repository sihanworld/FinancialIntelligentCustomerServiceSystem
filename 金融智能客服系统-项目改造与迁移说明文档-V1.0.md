# 金融智能客服系统 · 项目改造与迁移说明文档

> **文档定位**：本文档是企业级项目改造与迁移的总体规划与实施说明，覆盖从「电商智能客服系统（sz0331）」到「金融智能客服系统」的完整转型方案。内容包括现状盘点、金融业务数据服务（finance-data）深度分析、需求追溯、目标架构、数据库重建、YAML 流程重构、接口契约、前端改造、安全合规、实施计划、部署启动与验收标准。
>
> **结论先行**：原电商项目的「对话引擎内核（状态机 + YAML 流程推进 + 三轨规划）」具备业务无关性，可整体复用；需要重写的是 **YAML 流程定义、业务 Action（对接金融中台接口）、知识意图与提示词、数据库表结构、前端展示层**，并新增 **SSE 流式响应、会话管理接口、对话审计与可观测性** 能力。

---

## 文档信息

| 项 | 内容 |
|---|---|
| 文档名称 | 金融智能客服系统 · 项目改造与迁移说明文档 |
| 文档版本 | V1.0 |
| 编写日期 | 2026-08-27 |
| 文档状态 | 评审稿 |
| 适用读者 | 项目经理、后端开发、算法/提示词工程师、前端开发、测试、运维、业务验收方 |
| 源项目 | `D:\000_项目\0331_ecommerce_customer_service\sz0331-ecommerce-customer-service`（电商智能客服，YAML 流程驱动） |
| 金融业务资源 | `D:\000_AI_大模型_课件\尚硅谷大模型项目实战之电商小二实战\finance-data`（金融业务数据服务，92 张业务表 + 9 组 REST API） |
| 需求依据 | `D:\000_AI_大模型_课件\尚硅谷大模型项目实战之电商小二实战\1.资料\金融\需求说明.md` |
| 前端参考 | `D:\000_AI_大模型_课件\项目一智能客服\day01_项目介绍&初始化\4_other\中台+前端项目\前端服务\customer-service-frontend`（仅供参考，内容与风格需全面改造） |
| 配置基线 | `sz0331-ecommerce-customer-service\.env` 与 `finance-data\.env` |

### 修订记录

| 版本 | 日期 | 修订人 | 修订说明 |
|---|---|---|---|
| V1.0 | 2026-08-27 | 架构组 | 初稿：完成现状盘点、需求追溯、目标方案与实施计划 |

---

## 目录

1. [项目概述](#1-项目概述)
2. [源电商项目现状盘点](#2-源电商项目现状盘点)
3. [金融业务数据服务（finance-data）深度分析](#3-金融业务数据服务finance-data深度分析)
4. [需求追溯矩阵](#4-需求追溯矩阵)
5. [目标技术方案](#5-目标技术方案)
   - 5.1 目标架构与服务清单
   - 5.2 目标工程目录结构
   - 5.3 配置设计（.env 统一规范）
   - 5.4 数据库设计（业务库初始化 + 客服库重建 + 业务扩展表）
   - 5.5 YAML 流程重构（system_flows.yml / user_flows.yml 全量）
   - 5.6 Action 重构清单与中台接口映射
   - 5.7 知识轨重构（意图 / 提供者 / FAQ / 知识库）
   - 5.8 提示词模板重构（金融版）
   - 5.9 对话管控与状态模型
   - 5.10 客服后端接口契约（含 SSE 流式）
   - 5.11 前端改造方案
   - 5.12 安全与合规设计
   - 5.13 非功能设计（可靠性 / 可观测性 / 准确性 / 可扩展性）
6. [迁移实施计划](#6-迁移实施计划)
7. [部署与启动指南](#7-部署与启动指南)
8. [测试与验收方案](#8-测试与验收方案)
9. [附录](#9-附录)

---

## 1. 项目概述

### 1.1 项目背景

现有「电商智能客服系统」（工程代号 sz0331）是一套 **YAML 流程驱动** 的任务型对话系统：前端聊天窗口 → 客服后端（FastAPI + LangChain + LLM）→ 电商数据中台。其核心机制为：

- **三轨规划（TurnPlan）**：LLM 将每轮用户输入规划为 `task`（业务办理）/ `knowledge`（信息咨询）/ `chitchat`（闲聊）三条轨道；
- **YAML 流程引擎**：业务流程与系统流程全部由 `flow_config/*.yml` 声明（步骤：`start / end / collect / action`；跳转：静态边 / 条件边 / 兜底边），引擎按步骤推进并支持流程的中断、恢复、取消；
- **对话状态持久化**：`DialogueState`（激活任务、挂起任务栈、系统流程、会话与轮次、聚焦卡片）整体 JSON 化落库（`dialogue_states` 表），支持会话恢复。

因业务场景整体切换为 **金融行业**，需将上述系统改造为「金融智能客服系统」：服务对象从电商买家变为银行/金融平台客户，业务对象从订单/物流/退款变为 **账户、银行卡、信用卡、交易流水、贷款、理财、基金、存款、投诉工单**，外部依赖从电商中台切换为 **金融业务数据服务（finance-data）**。

### 1.2 项目目标

对照《金融智能客服系统项目实战需求说明》，本项目需达成：

1. **信息检索**：支持账户、银行卡、信用卡、存款、贷款、理财、基金、交易流水、转账记录等实时业务数据查询；支持利率说明、手续费规则、提前还款政策、信用卡还款规则、风险提示等 FAQ 检索；支持产品介绍、使用指南、金融政策、常见问题的知识库检索。
2. **任务型对话**：以多轮槽位收集方式完成 **账户查询、交易查询、贷款申请、信用卡挂失、投诉工单** 五类核心任务，并返回业务编号（申请编号 / 工单编号）。
3. **对话管控**：槽位主动追问、上下文保持、流程切换、流程恢复、流程取消。
4. **闲聊兜底**：非业务输入自然回复并适度引导回业务场景。
5. **交互能力**：文本消息 + 业务对象消息（点击卡片自动填槽）；非流式与 **SSE 流式** 双响应模式；会话创建、历史查询、状态查询、持久化与恢复；多意图/模糊输入下的 **意图澄清**。
6. **非功能**：单轮失败不影响整体服务、外部 API 超时重试与降级、全链路可观测（意图识别 → 规划 → 工具调用 → 工作流节点 → 回复生成 → 状态更新）、配置化扩展。

### 1.3 改造范围界定

| 范围 | 说明 |
|---|---|
| 在范围内 | 客服后端（YAML 流程、Action、知识轨、提示词、接口层、数据库表）、金融业务库初始化与扩展、前端演示/调试页面改造、部署文档、测试验收 |
| 不在范围内 | finance-data 既有 92 张表的核心模型重构（仅允许向后兼容的增量扩展）、LLM 模型训练、生产级网关/监控平台搭建 |
| 约束 | 所有数据库地址、端口、账号、密码 **一律以配置文件（.env）提供的参数为准**，代码中禁止硬编码；密码仅存在于配置文件/密钥管理，不进入代码与文档正文 |

### 1.4 用户角色

| 角色 | 职责 | 系统触点 |
|---|---|---|
| 客户 | 咨询金融产品、查询账户/交易、办理贷款申请、信用卡挂失、提交投诉 | 前端聊天窗口（登录态 = 客户号 `customer_no`） |
| 客服人员 | 处理系统无法自动完成的复杂业务、风险审核、投诉处理、人工兜底 | 人工坐席（本期通过 `human_handoff` 流程占位 + 工单流转承接） |
| 运营人员 | 维护 FAQ、知识库、YAML 流程配置与金融产品信息 | 配置文件 / 数据库维护表（`cs_faq`、`cs_knowledge_doc`、`flow_config/*.yml`） |

### 1.5 总体架构（目标态）

```
┌──────────────────────────────────────────────────────────────────────────────┐
│                              前端（Vue3 + Vite）                               │
│   金融风格聊天窗口：气泡消息 / 业务对象卡片 / 意图澄清 / SSE 流式渲染 / 人工转接      │
└───────────────────────────────┬──────────────────────────────────────────────┘
                                │ HTTP（/api/*，Vite 代理）
┌───────────────────────────────▼──────────────────────────────────────────────┐
│                  客服后端 finance-customer-service（FastAPI，:18082）          │
│  api 层（/api/chat、/api/chat/stream[SSE]、/api/chat/history、/api/sessions）  │
│  ┌─────────────── DialogueEngine（复用）──────────────┐                        │
│  │ TurnPlanner(LLM 三轨规划) → TurnPlanValidator(澄清) │                       │
│  │ ├─ TaskHandler：CommandProcessor + FlowExecutor(YAML 推进) + ActionRunner   │
│  │ ├─ KnowledgeHandler：意图→Provider 检索→LLM 组织回复                         │
│  │ ├─ ChitChatHandler：闲聊兜底            ├─ ClarifyResponder：意图澄清        │
│  └────────────────────────────────────────────────────┘                        │
│  Repository：DialogueState 持久化（客服库，MySQL）                              │
└───────────┬──────────────────────────────────────────────┬───────────────────┘
            │ httpx AsyncClient（Bearer 客户号 + 渠道头 + 幂等号）│
┌───────────▼───────────────────────────┐      ┌───────────▼──────────────────┐
│  金融业务数据服务 finance-data（:8000） │      │   LLM 网关（OpenAI 兼容）       │
│  基础/客户/账户交易/理财/信贷/还款/风控/  │      │   qwen-plus（.env 配置）        │
│  催收/运营支撑 共 9 组 REST API          │      └──────────────────────────────┘
└───────────┬───────────────────────────┘
┌───────────▼───────────────────────────────────────────────────────────────────┐
│                       MySQL（.env 提供连接参数）                                 │
│   ① finance 业务库：92 张金融业务表 + 扩展表（信用卡/存款/基金/AI 渠道）           │
│   ② customer_service 客服库：对话状态、消息流水、FAQ、知识库、审计追踪（重建）       │
└────────────────────────────────────────────────────────────────────────────────┘
```

**服务清单（目标态）**：

| 服务 | 工程 | 默认端口 | 职责 |
|---|---|---|---|
| 客服后端 | `finance-customer-service`（由 sz0331 改造） | 18082（.env `APP_PORT`） | 对话管理、意图规划、YAML 流程推进、调用 LLM 与金融中台 |
| 金融业务数据服务 | `finance-data` | 8000（.env `APP_PORT`） | 金融业务数据与业务操作 REST API（客户/账户/交易/理财/信贷/还款/风控/催收/运营） |
| 前端 | `finance-cs-frontend`（由 customer-service-frontend 改造） | 5174（Vite dev） | 金融风格聊天窗口与调试演示页 |
| MySQL | 由配置文件指定（默认 127.0.0.1:3306） | 3306 | `finance` 业务库 + `customer_service` 客服库 |

---

## 2. 源电商项目现状盘点

> 源工程：`sz0331-ecommerce-customer-service`。本章结论用于第 5 章的「复用 / 重构 / 新增」决策。

### 2.1 技术栈

| 层 | 技术 | 备注 |
|---|---|---|
| 运行时 | Python >= 3.12，uv 依赖管理 | `pyproject.toml` |
| Web 框架 | FastAPI + uvicorn（异步 ASGI） | `api/app.py` lifespan 管理资源 |
| 参数校验 | Pydantic v2 / pydantic-settings | 接口模型 + `.env` 配置 |
| ORM / 持久化 | SQLAlchemy 2.x async + aiomysql → MySQL | 仅 1 张 `dialogue_states` 表 |
| LLM 调用 | langchain + langchain-openai（`init_chat_model`，OpenAI 兼容协议） | 指向 `.env` 的 `LLM_BASE_URL` |
| 流程定义 | PyYAML 加载 `flow_config/*.yml`，Jinja2 渲染响应文本 | 本项目改造重点 |
| 中台通信 | httpx AsyncClient（timeout=120s） | 改造为对接金融中台 |

### 2.2 工程目录与模块职责

```
sz0331-ecommerce-customer-service/
├── main.py                      # uvicorn 启动入口（app="api.app:app"）
├── pyproject.toml / uv.lock     # 依赖（fastapi/sqlalchemy/langchain/pyyaml/httpx 等）
├── .env                         # LLM / 数据库 / 中台地址 / 端口（★ 改造基线）
├── flow_config/                 # ★ YAML 流程定义（★ 全量重写）
│   ├── system_flows.yml         #   系统流程：任务开始/收集信息/恢复/恢复失败/中断/取消/无法处理
│   └── user_flows.yml           #   业务流程 + 槽位定义（订单/物流/退款/推荐/人工）
└── atguigu/                     # 主包（建议改名，如 financecs/）
    ├── api/                     # Web 层：app.py(lifespan)、chat_router.py、schemas.py、dependencies.py
    ├── config/settings.py       # pydantic-settings 读取 .env
    ├── domain/                  # 领域模型：messages.py / state.py(DialogueState) / contexts.py
    ├── engines/                 # dialogue_engine.py（消息分流总控）、builder.py（装配）
    ├── plan/                    # planner.py（LLM 三轨规划）、turn_plan.py、validator.py（澄清判定）
    ├── task/                    # 任务轨：
    │   ├── commands/            #   Command 模型 + CommandProcessor（start/resume/cancel/set_slots）
    │   ├── flows/               #   loader.py(YAML 装载)、flows.py、steps.py、links.py、executor.py(推进器)
    │   ├── action/              #   base/runner/register/builder + builtin(response,listen) + customer(3 个电商 Action)
    │   └── handler.py           #   TaskHandler = 改状态 + 推流程 + 跑 Action
    ├── knowledge/               # 知识轨：intents.py(意图表)、handler.py、resonder.py、provider/(api/faq/rag)
    ├── chitchat/                # 闲聊轨：handler.py、responder.py
    ├── clarify/responder.py     # 意图澄清话术改写
    ├── chat_history/builder.py  # 历史对话序列化（供提示词）
    ├── prompt/                  # loader.py + jinja2/（turn_plan/knowledge_respond/chitchat_respond/clarify_respond）
    ├── repository/              # DialogueRepository：load_state/save_state（INSERT ... ON DUPLICATE KEY UPDATE）
    ├── infrastructure/          # db_client.py / http_client.py / llm_client.py
    ├── services/dialogue_service.py  # 三步编排：读状态 → 引擎处理 → 存状态
    └── test/
```

### 2.3 核心机制（改造时必须保持语义一致的资产）

#### 2.3.1 三轨规划（TurnPlan）

`TurnPlanner` 以 `turn_plan.jinja2` 提示词驱动 LLM 输出 JSON：

```json
{
  "task": { "commands": [ {"command": "start_flow", "flow": "<flow_id>"}, {"command": "set_slots", "slots": {"<slot>": "<value>"}}, {"command": "resume_flow"}, {"command": "cancel_flow"} ] },
  "knowledge": { "intents": ["<intent>"] },
  "chitchat": { "chat": "用户的问题" }
}
```

- 提示词中注入 `available_flows_json`（YAML 中非 `system_` 前缀流程的 id/name/description/缺失槽位）、`knowledge_intents_json`、`active_task_json`、`interrupted_tasks_json`、`focused_object_json` 与近 10 轮历史；
- `TurnPlanValidator` 校验失败时按 `ClarifyReason`（缺轨道 / 多轨道 / 缺命令 / 未知流程 / 缺聚焦对象等 9 类）触发澄清；
- 执行顺序：task 优先，其次 knowledge，最后 chitchat；多轨道同时命中时先澄清。

> **改造结论**：机制原样复用；仅需重写 `turn_plan.jinja2` 中的领域表述（电商 → 金融），流程清单与意图清单随 YAML/意图表自动注入，无需改代码。

#### 2.3.2 YAML 流程引擎

- **步骤类型**：`start` / `end` / `collect`（槽位收集，含 `response` 追问话术与可选 `validated` 校验）、`action`（执行 Action）；
- **跳转边**：字符串=静态边；`[{if: "<表达式>", then: <step>}, ..., {else: <step>}]`=条件边 + 兜底边；
- **响应模式**（`action_response` 的 args）：`mode: static`（Jinja2 模板渲染 `{{ slots.x }}` / `{{ context.x }}`）、`mode: rephrase`（先渲染再由 LLM 润色）、`mode: generate`（LLM 直接生成）；
- **推进规则**（`FlowExecutor`）：双层循环推进直至遇到 `action_listen`（等待用户输入）；`collect` 步骤在当前缺失槽位与「聚焦卡片」类型匹配时可自动填槽（电商映射：`order→order_number`、`product→product_id`）；
- **系统流程**（`system_flows.yml`）：`system_task_started / system_collect_information / system_task_resumed / system_task_resume_failed / system_task_interrupted / system_task_canceled / system_cannot_handle`，承担过场白与异常兜底。

> **改造结论**：引擎与步骤模型复用；`collect` 自动填槽的「卡片类型 → 槽位」映射表需从电商对象改为金融对象（见 5.9.3）；两份 YAML 全量重写（见 5.5）。

#### 2.3.3 对话状态与持久化

- `DialogueState`：`active_task`（当前业务流程上下文：flow_id/step_id/slots）、`paused_tasks`（挂起栈，支持中断-恢复）、`active_system_task`、`sessions/turns`（会话与轮次消息）、`focused_object`（用户点击的卡片）、`pending_turn`（当前轮缓冲）；
- 持久化：整体 `to_dict()` → JSON → `dialogue_states(sender_id PK, state_json TEXT)`，`INSERT ... ON DUPLICATE KEY UPDATE` 幂等写回；
- 服务编排：`DialogueStateService.process_message = load_state → engine.handle_message → save_state`。

> **改造结论**：模型与编排复用；`dialogue_states` 表在客服库重建（增加 `customer_no`、乐观锁版本与时间戳），并新增消息流水/审计表（见 5.4）。

#### 2.3.4 对外接口（现状）

| 接口 | 方法 | 契约要点 |
|---|---|---|
| `/api/chat` | POST | 请求 `{sender_id, text?, object?{id,title,type,attributes}}`；响应 `{message_id, messages:[{text, object?}]}` |
| `/api/chat/history` | GET | `?sender_id=`，返回按会话组织的 `{session_id, role, text, object?}` 列表 |
| `/`、`/test` | GET | 健康/测试占位（改造时替换为 `/health`） |

> **改造结论**：`/api/chat` 契约兼容保留；新增 SSE、会话管理、状态查询接口（见 5.10）。

### 2.4 现有配置（.env）与去向

| 现有配置 | 现值来源 | 目标处理 |
|---|---|---|
| `LLM_MODEL=qwen-plus` | 保留 | 沿用（可按网关能力升级） |
| `LLM_BASE_URL` / `LLM_API_KEY` | 保留 | 沿用（仅存于配置文件，禁止入库入码） |
| `COMMERCE_API_BASE_URL` | 电商中台 | **删除**，替换为 `FINANCE_API_BASE_URL`（指向 finance-data，默认 `http://127.0.0.1:8000`，以配置文件为准） |
| `DATABASE_URL` | 客服对话状态库 | **重命名**为 `CS_DATABASE_URL`，指向重建后的客服库；连接参数以配置文件为准 |
| `APP_HOST` / `APP_PORT` | 保留 | 沿用（默认 0.0.0.0:18082） |

### 2.5 复用性结论总表

| 模块 | 决策 | 说明 |
|---|---|---|
| engines / plan / task(commands,flows 引擎) / domain / services / repository 骨架 / chat_history / clarify / chitchat 框架 | **复用** | 业务无关的对话内核 |
| `flow_config/*.yml` | **重写** | 金融流程 + 槽位（5.5） |
| `task/action/customer/*` | **重写** | 电商 3 个 Action 删除，新建金融 Action（5.6） |
| `knowledge/intents.py` + providers | **重写** | 金融知识意图 + 金融中台 provider（5.7） |
| `prompt/jinja2/*` | **重写** | 金融人设与领域约束（5.8） |
| `api/schemas.py` / `chat_router.py` | **增强** | 增加 SSE、会话、状态接口，对象卡片类型扩展 |
| `config/settings.py` / `.env` | **增强** | 新增金融中台、渠道、重试、会话过期等配置项 |
| 数据库 `dialogue_states` | **重建** | 客服库重建 6 张表（5.4） |
| 前端 | **重构** | 金融风格与新卡片组件（5.11） |

---

## 3. 金融业务数据服务（finance-data）深度分析

> 资源路径：`...\尚硅谷大模型项目实战之电商小二实战\finance-data`。该服务是改造后客服系统的 **唯一业务中台**（替代电商中台），其数据模型与接口约定是 Action 设计的直接依据。

### 3.1 服务概况

| 项 | 内容 |
|---|---|
| 服务名 | Finance Data API（FastAPI，`app/main.py`） |
| 默认端口 | `8000`（`.env` 的 `APP_PORT`，可配置） |
| 数据库 | MySQL 库 `finance`（92 张表，`sql/finance.sql`，utf8mb4 / InnoDB） |
| 初始化 | `init_db.py`：检测→删除→创建 `finance` 库→按分号拆分执行 `sql/finance.sql` |
| 数据生成 | `generate/`：9 层（Layer1 基础配置与产品主数据 → Layer2 客户主数据 → Layer3 账户与交易对账 → Layer4 理财闭环 → Layer5 信贷授信放款 → Layer6 还款逾期减免 → Layer7 风控反洗钱催收 → Layer8 运营支撑与日统计 → 最终验收）；`make gen`（全量）/ `make smoke`（小规模） |
| 种子数据 | `seeds/1_foundation`（机构/渠道/币种/风险等级/员工/产品分类维表）、`2_product`（账户/贷款/理财/服务产品）、`3_rule`（风控策略规则/指标字典） |
| 接口测试 | `tests/test_api_business_flows.py`、`tests/test_api_route_coverage.py`（pytest 全链路） |
| 连接参数 | 一律来自 `.env`：`DB_HOST=127.0.0.1`、`DB_PORT=3306`、`DB_USER=root`、`DB_PASSWORD=123321`、`DB_NAME=finance`、`APP_PORT=8000` |

### 3.2 数据模型总览（92 张表 / 9 大域）

| 域 | 代表表 | 客服相关性 |
|---|---|---|
| 基础维度 | `dim_branch`(机构)、`dim_channel`(渠道)、`dim_currency`(币种)、`dim_risk_level`(风险等级)、`dim_employee`(员工)、`dim_product_category`(产品分类)、`account_product`(账户产品)、`service_product`(服务产品) | ★★★ 咨询类问题的规则来源（限额/费率/风险等级口径） |
| 客户域 | `customer`、`customer_identity`、`customer_contact`、`customer_kyc`、`customer_risk_assessment`、`customer_tag(_rel)`、`customer_status_history`、`customer_device`、`enterprise_profile`、`beneficial_owner` | ★★★ 身份识别、身份验证槽位、状态准入校验 |
| 账户交易域 | `bank_account`、`bank_card`、`account_transaction`、`channel_transaction`、`account_ledger`、`fund_freeze(_operation)`、`reconciliation_batch/result/adjustment`、`bank_account_status_history` | ★★★ 账户查询 / 交易查询 / 挂失的核心表 |
| 理财域 | `wealth_product`、`wealth_nav`(净值)、`wealth_position`(持仓)、`wealth_order`(申购赎回)、`wealth_income`、`wealth_open_period`、`wealth_trade_calendar`、`wealth_settlement_rule`、`wealth_product_notice` | ★★ 理财产品咨询、持仓收益查询（扩展） |
| 信贷域 | `loan_product`、`loan_product_eligibility_rule`、`loan_product_rate_tier`、`loan_product_required_material`、`credit_application(_material)`、`credit_assessment`、`credit_limit`、`loan_application(_material)`、`loan_approval_record`、`loan_contract(_document)`、`contract_sign_record`、`collateral_asset`、`guarantee_record`、`loan_disbursement` | ★★★ 贷款咨询与贷款申请任务 |
| 还款逾期域 | `repayment_schedule`、`repayment_bill`、`repayment_record`、`repayment_allocation`、`repayment_authorization`、`overdue_record`、`fee_reduction` | ★★ 还款政策咨询、还款/逾期查询（扩展） |
| 风控催收域 | `risk_strategy`、`risk_rule`、`risk_strategy_rule_rel`、`risk_event`、`risk_hit_record`、`blacklist_record`、`aml_case(_transaction)`、`suspicious_transaction_report`、`aml_review_result`、`manual_review_task`、`collection_case`、`collection_action`、`collection_contact_record`、`repayment_promise`、`legal_case`、`loan_write_off`、`loan_restructure`、`collateral_disposal`、`collection_performance_daily` | ★ 风险提示话术来源（客服仅读规则口径，不直接暴露风控明细） |
| 运营支撑域 | `workflow_instance`、`workflow_task`、`notification_message`、`support_ticket`、`support_ticket_feedback`、`business_metric_dict`、`business_stat_daily` | ★★★ 投诉工单任务直接写入 `support_ticket` |

### 3.3 客服高频核心表字段速览

#### 3.3.1 `customer`（客户主表）

`customer_no`(业务唯一)、`customer_type`(personal/enterprise)、`customer_name`、`branch_id`、`register_channel_id`、`risk_level_id`、`customer_status`(`pending_kyc` 待实名 / `active` 正常 / `restricted` 限制 / `frozen` 冻结 / `closed` 销户)、`opened_at`。

> 业务约束：限制/冻结/销户客户不得新增账户、贷款申请、理财申购与支出类交易；状态为 `closed/cancelled/blacklisted` 时中台接口直接返回 403。

#### 3.3.2 `bank_account`（银行账户）

`account_no`(唯一)、`customer_id`、`account_type`(`demand_deposit`/`settlement`)、`account_status`(`active`/`frozen`/`restricted`/`closed`)、`balance_amount`(余额)、`frozen_amount`(冻结金额)、`available_amount`(可用余额)、`currency_code`、`account_product_id`。

#### 3.3.3 `bank_card`（银行卡）

`card_no`(唯一)、`customer_id`、`account_id`、`card_type`(`debit` 借记卡 / `virtual` 虚拟卡)、`card_level`(`standard`/`gold`/`platinum`/`diamond`)、`card_status`(`active`/`frozen`/`lost` 挂失/`expired`/`cancelled`)、`issued_at`、`expired_at`。

> 挂失语义：`card_status=lost` 后该卡不能发起新交易；`cancelled` 不可恢复。这是「信用卡挂失」任务的落库目标（信用卡扩展见 5.4.3）。

#### 3.3.4 `account_transaction`（账户交易）

`transaction_no`(唯一)、`customer_id`、`from_account_id`/`to_account_id`、`card_id`、`channel_id`、`transaction_type`(`transfer` 转账 / `consume` 消费 / `deposit` 入金 / `withdraw` 出金 / `loan_disbursement` / `loan_repayment` / `wealth_purchase` / `wealth_redeem` / `wealth_income` / `refund` / `cancel` / `reversal` / `adjustment`)、`transaction_status`(`created`/`processing`/`success`/`failed`/`reversed`)、`reconcile_status`、`currency_code`、`transaction_amount`、`fee_amount`、`counterparty_name/account_no/bank_name`、`merchant_name`、`transaction_at`。

#### 3.3.5 `loan_product` / `loan_application`（贷款产品 / 贷款申请）

- `loan_product`：`product_code`、`loan_type`(`consumer` 等)、`min_amount`/`max_amount`、`min_term_months`/`max_term_months`、`annual_interest_rate`、`min/max_interest_rate`、`repayment_method`(如 `equal_principal_interest` 等额本息)、`collateral_required_flag`、`product_status`；
- `loan_application`：`application_no`、`customer_id`、`product_id`、`credit_limit_id`、`channel_id`、`apply_amount`、`apply_term_months`、`loan_purpose`、`application_status`(`created`/`risk_reviewing`/`manual_reviewing`/`approved`/`rejected`/`cancelled`/`expired`)、`risk_decision`(`pass`/`manual`/`reject`)、`submitted_at`、`expired_at`；
- 前置链路：`credit_application`(授信申请) → `credit_approval_record`(审批) → `credit_limit`(授信额度) → `loan_application`（必须携带有效 `limit_no`，金额不得超过可用额度，期限必须落在产品期限区间内）。

#### 3.3.6 `wealth_product`（理财产品）

`product_code`、`product_name`、`product_type`(`cash_management`/`fixed_income`/…)、`operation_mode`(`open`/`closed`)、`risk_level_id`(P1~P5)、`min_purchase_amount`(起购)、`increment_amount`(递增)、`expected_yield_rate`(预期收益率)、`nav_based_flag`、`value_date_rule`(T+1)、`redeem_rule`(T+1 到账/到期兑付)、`product_status`(`selling` 等)、`sale_start_at/sale_end_at`。

#### 3.3.7 `support_ticket`（客服工单）

`ticket_no`、`customer_id`、`channel_id`、`assignee_id`(处理员工)、`ticket_type`(`account_issue`/`transaction_issue`/`wealth_issue`/`loan_issue`/`repayment_issue`/`complaint`/`other`)、`related_type`(`none`/`account_transaction`/`wealth_order`/`loan_application`/`repayment_bill`/`support_ticket`)、`related_id`、`ticket_title`、`ticket_content`、`ticket_status`(`submitted`/`processing`/`waiting_customer`/`resolved`/`closed`/`rejected`)、`handle_result`、`submitted_at/handled_at/closed_at`；反馈表 `support_ticket_feedback`(确认状态/满意度 1~5/反馈内容)。

### 3.4 接口通用约定（客服后端必须遵守）

| 项 | 约定 |
|---|---|
| 数据格式 | `application/json`，UTF-8 |
| 金额 | 字符串传输，保留两位小数（如 `"1000.00"`） |
| 时间 | 日期 `YYYY-MM-DD`；时间为 ISO 8601 本地时间（服务端时区 `Asia/Shanghai`） |
| 认证 | 所有接口必须携带 `Authorization: Bearer <token>`；当前实现中 token 即 **客户号**（客户端）或 **员工编号**（员工端/系统间）。客户号必须存在于 `customer.customer_no` 且状态允许办理业务 |
| 渠道头 | `X-Channel-Code` **必填**，取值 `dim_channel.channel_code`（`MOBILE_BANK`/`ONLINE_BANK`/`COUNTER`/`OPEN_API`/`PARTNER_APP`/`BATCH_JOB`/`ALL`），渠道必须 `active` |
| 追踪头 | `X-Request-Id`（请求追踪号，回传于响应 `request_id`）；`X-Operator-No`（员工人工操作时必填，客服侧客户自助场景不传） |
| 幂等 | 资金/订单/申请/审批/还款/调账类 **写接口必须传 `request_no`**；相同 `渠道+业务类型+request_no` 重复提交返回首次结果；同一幂等号更换请求体会返回 409 `IDEMPOTENCY_PAYLOAD_MISMATCH` |
| 分页 | `page_no`(从 1 起) / `page_size`(1~100)，列表响应含 `list`、`total_count` |
| 响应包裹 | `{ "code": 0, "message": "ok", "request_id": "...", "data": {...} }`；失败时 `code` 为大写错误码字符串（如 `CUSTOMER_NOT_FOUND`） |
| 归属校验 | 客户身份只能访问本人客户号、账户、订单、合同、账单、案件；越权返回 403 |

### 3.5 客服 Action 将使用的中台接口清单

| 能力 | 方法 + 路径 | 关键入参 | 关键出参（data） |
|---|---|---|---|
| 客户详情 | `GET /api/v1/customers/{customer_no}` | 路径客户号 | 客户基本信息、状态 |
| 客户账户列表 | `GET /api/v1/customers/{customer_no}/accounts` | 路径客户号 | 账户列表（卡号→账户号解析依据） |
| 账户详情（余额） | `GET /api/v1/accounts/{account_no}` | 路径账户号 | `account_status`、`balance_amount`、`frozen_amount`、`account_product` |
| 账户交易明细 | `GET /api/v1/accounts/{account_no}/transactions` | `start_time`、`end_time`、`transaction_type`、`transaction_status`、`page_no`、`page_size` | `list`(交易流水)、`total_count` |
| 交易详情 | `GET /api/v1/transactions/{transaction_no}` | 路径交易号 | `transaction_status`、`amount`、`channel_transaction`、`reconcile_status` |
| 贷款产品列表 | `GET /api/v1/loan/products` | `loan_type`、`currency_code`、`product_status` | 产品代码、利率区间、期限区间 |
| 贷款产品详情 | `GET /api/v1/loan/products/{product_code}` | 路径产品代码 | 产品全量属性、准入规则、利率档位、所需材料 |
| 客户授信额度 | `GET /api/v1/customers/{customer_no}/credit-limits` | 路径客户号 | 有效额度（贷款申请的 `limit_no` 来源） |
| 提交贷款申请 | `POST /api/v1/loan/applications` | `request_no`、`customer_no`、`limit_no`、`apply_amount`、`apply_term_months`、`repayment_method`、`loan_purpose`、`materials[]` | `application_no`、`application_status` |
| 贷款申请详情 | `GET /api/v1/loan/applications/{application_no}` | 路径申请号 | 申请状态、金额、期限、产品 |
| 理财产品列表 | `GET /api/v1/wealth/products` | `risk_level_code`、`currency_code`、`product_status` | `product_code`、`product_name`、`risk_level`、`expected_yield_rate`、`open_status` |
| 理财产品详情 | `GET /api/v1/wealth/products/{product_code}` | 路径产品代码 | 产品全量属性 |
| 产品净值 | `GET /api/v1/wealth/products/{product_code}/navs` | 路径产品代码 | 净值序列 |
| 创建客服工单 | `POST /api/v1/support/tickets` | `request_no`、`customer_no`、`ticket_type`、`ticket_title`、`ticket_content`、`related_type`、`related_id?` | `ticket_no`、`ticket_status=submitted`（同时自动创建 `support_ticket` 工作流） |
| 工单详情 | `GET /api/v1/support/tickets/{ticket_no}` | 路径工单号 | `ticket_info`、`handler`、`process_result`、`feedback` |
| 工单反馈 | `POST /api/v1/support/tickets/{ticket_no}/feedback` | `request_no`、`confirm_status`、`satisfaction_score`、`feedback_content` | `feedback_no` |
| 基础字典 | `GET /api/v1/branches|channels|currencies|risk-levels|account-products|service-products|employees` | 可选过滤 | 维度数据（话术口径） |
| 通知消息 | `POST /api/v1/notifications`、`GET /api/v1/customers/{customer_no}/notifications` | 消息类型/渠道 | 受理结果/消息列表 |
| 健康检查 | `GET /health` | 无 | `{status:"ok"}` |

### 3.6 与需求对照的差距分析（Gap Analysis）

需求说明要求的业务对象与 finance-data 现状对照：

| 需求对象 | finance-data 现状 | 差距与对策 |
|---|---|---|
| 银行账户 / 银行卡 / 交易流水 / 转账记录 | ✅ 完整 | 直接使用 |
| 贷款产品 / 贷款申请 / 授信 | ✅ 完整（含额度前置链路） | 客服侧「贷款申请」任务需先查 `credit-limits` 取 `limit_no`；无额度时引导授信申请或转人工 |
| 理财产品 / 基金产品 / 净值 / 持仓 | ✅ 理财完整；❌ 无独立「基金」实体 | 一期将基金咨询并入理财口径（`wealth_product`）；二期新增 `fund_product` 扩展表与接口（5.4.3） |
| 存款产品（利率/起存/存期） | ⚠️ 无独立存款产品表（结构性存款归入理财 `WEALTH_STRUCTURED`） | 二期新增 `deposit_product` 扩展表；一期用 `account_product` + FAQ 承接利率/手续费咨询 |
| 信用卡（年费/权益/额度/挂失） | ⚠️ `bank_card` 仅 `debit`/`virtual`，无信用额度字段；无独立挂失接口 | 二期新增 `credit_card_product` / `credit_card` 扩展表；一期挂失任务复用 `bank_card`（`card_status=lost`）+ 中台扩一个「卡状态变更/挂失」接口（5.4.3、5.6） |
| 投诉工单 | ✅ `support_ticket` + 工作流 | 直接使用 |
| AI 客服渠道 | ⚠️ `dim_channel` 无智能客服渠道 | 扩展种子：新增 `AI_CS`（智能客服）渠道行，客服后端统一使用该渠道头（5.4.3） |
| 卡片号 → 账户解析 | ⚠️ 无从卡号查账户/客户的接口 | 中台扩展 `GET /api/v1/cards/{card_no}`；一期可退化为要求用户提供账户号（流程内澄清二选一） |

> **处置原则**：一期（本迭代）不改动 finance-data 核心模型，缺口通过「流程话术规避 + 扩展接口/扩展表」双轨解决；扩展内容全部向后兼容、可增量执行，详见 5.4.3 与 6.2 阶段任务。

---

## 4. 需求追溯矩阵

> 依据《金融智能客服系统项目实战需求说明》逐条落到实现方案，供验收对照（第 8 章）。

### 4.1 功能需求追溯

| 需求编号 | 需求描述 | 实现方案 | 落点（章节/工件） |
|---|---|---|---|
| F-01 | 实时业务数据查询：账户、银行卡、信用卡、存款、贷款、理财、基金、交易流水、转账记录 | 知识轨 `api.*` Provider + 任务轨查询流程，经客服后端统一调用金融中台 | 5.6（Action 清单）、5.7（意图） |
| F-02 | FAQ 检索：利率说明、手续费规则、提前还款政策、信用卡还款规则、风险提示 | 客服库 `cs_faq` 表 + `faq.default` Provider（关键词匹配，可升级向量检索） | 5.4.2、5.7 |
| F-03 | 知识库检索：产品介绍、使用指南、金融政策、常见问题 | 客服库 `cs_knowledge_doc` 表 + `rag.default` Provider（一期全文检索） | 5.4.2、5.7 |
| F-04 | 任务：账户查询（收集身份/银行卡号 → 查余额/可用/冻结） | 业务流程 `account_balance_query` + `action_query_account_balance` | 5.5、5.6 |
| F-05 | 任务：交易查询（收集账户号、交易日期 → 流水明细） | 业务流程 `transaction_query` + `action_query_transactions` | 5.5、5.6 |
| F-06 | 任务：贷款申请（收集类型/金额/期限/用途 → 提交 → 返回申请编号） | 业务流程 `loan_application` + 查额度/提交申请 Action | 5.5、5.6 |
| F-07 | 任务：信用卡挂失（收集卡号/挂失原因/身份验证 → 提交 → 反馈结果） | 业务流程 `credit_card_loss_report` + `action_report_card_loss` | 5.5、5.6 |
| F-08 | 任务：投诉工单（收集类型/关联交易号/问题描述 → 创建工单 → 返回工单编号） | 业务流程 `complaint_ticket` + `action_create_ticket` | 5.5、5.6 |
| F-09 | 槽位收集（主动追问缺失信息） | `collect` 步骤 + `system_collect_information` 系统流程 | 5.5 |
| F-10 | 上下文保持（客户身份、卡号、账户、交易等） | `DialogueState.active_task.slots` + 会话级 `customer_no` 绑定 + 槽位继承策略 | 5.9 |
| F-11 | 流程切换 / 恢复 / 取消 | 复用挂起栈机制 + `resume_flow/cancel_flow` 命令 + 系统过场流程 | 5.5（系统流程）、2.3.2 |
| F-12 | 闲聊兜底并适度引导回业务 | `chitchat` 轨道 + 金融版 `chitchat_respond.jinja2` | 5.8 |
| F-13 | 文本消息 + 业务对象消息；对象消息自动填槽 | `/api/chat` 的 `object` 字段；卡片类型映射（bank_card/account/transaction/loan_product/wealth_product） | 5.9.3、5.10 |
| F-14 | 非流式 + SSE 流式响应 | `POST /api/chat`（保持）+ `POST /api/chat/stream`（新增） | 5.10 |
| F-15 | 会话管理：新建会话、历史消息、当前状态查询、持久化、恢复 | `POST /api/sessions`、`GET /api/chat/history`、`GET /api/sessions/state`、状态落库与会话过期策略 | 5.10、5.4.2 |
| F-16 | 意图澄清（多意图/模糊/缺关键信息/上下文冲突） | `TurnPlanValidator` 9 类 `ClarifyReason` + `ClarifyResponder` + `system_cannot_handle` | 2.3.1、5.8 |
| F-17 | 交付物：对话接口、金融样本数据与配置、前端演示页、项目文档 | 客服后端接口、finance-data 初始化与生成脚本、改造前端、本文档 | 全部 |

### 4.2 非功能需求追溯

| 需求编号 | 需求描述 | 实现方案 |
|---|---|---|
| N-01 | 单次对话失败不影响整体服务 | 引擎层按轮隔离异常：任一轨道异常降级为 `system_cannot_handle` 话术；状态保存失败降级为内存态继续应答并告警 |
| N-02 | 外部金融 API 超时/重试/异常处理 | httpx 统一封装：连接超时 5s/读取 30s，幂等写接口不自动重试、读接口最多重试 2 次（指数退避），失败转人工话术 |
| N-03 | 状态保存失败降级 | `save_state` 异常捕获 → 记录 `cs_action_audit` 错误日志 → 返回响应不阻断 |
| N-04 | 全链路可观测 | `cs_turn_trace`（每轮规划/澄清/耗时）+ `cs_action_audit`（工具调用入参出参）+ 结构化日志（request_id 贯穿）+ 中台 `X-Request-Id` 透传 |
| N-05 | 准确性：实时数据、槽位准确、知识优先、符合业务规则 | 查询一律经中台实时接口；槽位 `validated` 校验（卡号/账户号格式、金额区间）；知识回复提示词强约束「只依据检索内容」 |
| N-06 | 可扩展：新流程配置化、新知识类型、新 API、新金融产品（保险/证券/外汇） | 新流程=加 YAML+注册 Action；新知识=加意图+Provider；新产品域=扩展表+接口+意图，不改引擎 |

### 4.3 典型场景对话设计（验收演示脚本基线）

| 场景 | 对话脚本（示意） | 命中机制 |
|---|---|---|
| 产品咨询 | 「有没有适合稳健投资的理财产品？」→ 返回产品列表/风险等级/预期收益，引导查看详情 | knowledge：`wealth_product_info` → `api.wealth_product` |
| 产品对比 | 「信用卡金卡和白金卡有什么区别？」→ 额度/权益/年费/适用人群 | knowledge：`credit_card_info` → `faq.default`+`rag.default`（二期 `api.credit_card_product`） |
| 账户查询 | 「帮我查一下账户余额」→ 追问账户号/卡号 → 返回余额/可用/冻结 | task：`account_balance_query` |
| 交易查询 | 「帮我查一下昨天的消费记录」→ 追问卡号/账户号 → 返回昨日流水 | task：`transaction_query`（`set_slots` 自动带出日期语义） |
| 贷款申请 | 「我要申请消费贷款」→ 依次收集金额/期限/用途 → 提交 → 返回申请编号与审核说明 | task：`loan_application` |
| 信用卡挂失 | 「我的信用卡丢了」→ 收集卡号/原因 → 身份验证 → 提交挂失 → 成功反馈 | task：`credit_card_loss_report` |
| 投诉工单 | 「我的转账一直没有到账」→ 收集交易流水号/问题描述 → 创建工单 → 返回工单编号与预计处理时间 | task：`complaint_ticket` |
| 流程切换 | 贷款办理中插入「先帮我查下余额」→ 挂起贷款流程 → 查完自动询问是否继续贷款 | `start_flow` 中断 + `system_task_interrupted` + `resume_flow` |

---

## 5. 目标技术方案

### 5.1 目标架构与服务清单

目标态服务拆分与职责见 1.5。关键架构决策：

| 决策点 | 决策 | 理由 |
|---|---|---|
| 对话内核 | 保留 sz0331 引擎（状态机 + YAML 推进 + 三轨规划） | 业务无关，回归测试成本最低 |
| 业务数据来源 | 客服后端 **不直连** `finance` 业务库，一律经 finance-data REST API | 保证业务规则（幂等/归属/状态机）由中台统一执行，客服侧只读编排 |
| 客服自有数据 | 独立 `customer_service` 库存放对话状态、消息、知识、审计 | 与业务库隔离，重建不影响业务数据 |
| 身份模型 | `sender_id` 与金融客户号 `customer_no` 合一 | 中台所有接口以 `Authorization: Bearer <customer_no>` 为客户登录态 |
| 渠道标识 | 客服调用中台统一携带 `X-Channel-Code: AI_CS`（扩展渠道） | 便于中台对账、审计区分智能客服来源 |
| 幂等号策略 | 写接口 `request_no = CS{sender_id 短码}{flow_id}{turn 序号}{业务动作}`（UUID 兜底） | 同轮重试不产生重复申请/工单 |
| 响应模式 | 非流式默认；SSE 用于知识/闲聊/润色类 LLM 回复 | 满足 5.2 响应模式需求 |

### 5.2 目标工程目录结构

```
finance-customer-service/                 # 由 sz0331 改造（建议重命名工程与主包）
├── main.py                               # 启动入口（不变）
├── pyproject.toml                        # 依赖（不变，版本保持）
├── .env                                  # ★ 统一配置（5.3）
├── sql/                                  # ★ 新增：客服库建表脚本
│   └── customer_service.sql              #   客服库 6 张表 DDL（5.4.2）
├── seeds/                                # ★ 新增：FAQ / 知识库 / 扩展字典种子
│   ├── cs_faq.csv
│   └── cs_knowledge_doc.csv
├── flow_config/                          # ★ 重写：金融流程
│   ├── system_flows.yml                  #   系统流程（金融话术版）
│   └── user_flows.yml                    #   金融业务流程 + 槽位定义
├── scripts/                              # ★ 新增：运维脚本
│   ├── init_cs_db.py                     #   客服库初始化（执行 sql/）
│   └── import_seeds.py                   #   FAQ/知识种子导入
└── financecs/                            # 主包（atguigu → financecs）
    ├── api/                              # 增强：chat_router（+SSE/会话/状态）、schemas 扩展
    ├── config/settings.py                # 增强：金融中台与治理配置项
    ├── domain/                           # 复用：messages/state/contexts
    ├── engines/                          # 复用：dialogue_engine/builder
    ├── plan/                             # 复用：planner/turn_plan/validator
    ├── task/
    │   ├── commands/ flows/              # 复用（collect 自动填槽映射表更新为金融对象）
    │   └── action/
    │       ├── builtin/                  # 复用：action_response / action_listen
    │       └── finance/                  # ★ 重写（原 customer/ 删除）：金融 Action 集（5.6）
    ├── knowledge/                        # ★ 重写：intents/handler/responder/provider
    │   └── provider/                     #   api.loan_product / api.wealth_product / api.account / faq.db / rag.db
    ├── chitchat/ clarify/                # 复用（提示词更换）
    ├── chat_history/                     # 复用（对象标签扩展）
    ├── prompt/jinja2/                    # ★ 重写 4 个模板（5.8）
    ├── repository/                       # 增强：对话状态 + 消息流水 + 审计落库
    ├── infrastructure/                   # 增强：
    │   ├── db_client.py                  #   客服库引擎
    │   ├── finance_client.py             #   ★ 新增：金融中台客户端（鉴权头/重试/超时/审计）
    │   └── llm_client.py                 #   复用
    ├── services/dialogue_service.py      # 增强：状态保存降级、消息流水记录
    └── test/                             # 新增：流程级回归用例
```

finance-data 侧（最小增量，见 5.4.3）：

```
finance-data/
├── sql/ext/
│   ├── 001_channel_ai_cs.sql             # dim_channel 增加 AI_CS 渠道
│   ├── 002_credit_card.sql               # credit_card_product / credit_card（二期）
│   ├── 003_deposit_fund.sql              # deposit_product / fund_product（二期）
│   └── 004_card_endpoints.md             # 卡查询/挂失扩展接口说明
└── app/routers/cards.py                  # ★ 新增：GET /cards/{card_no}、POST /cards/{card_no}/loss-report
```

### 5.3 配置设计（.env 统一规范）

**原则**：所有 IP、端口、账号、密码、密钥只存在于 `.env`（或部署平台的密钥管理），代码通过 `pydantic-settings` 读取；`.env` 不入 Git（沿用 `.gitignore`）。参数取值以两份既有配置文件为准。

#### 5.3.1 客服后端 `.env`（目标模板）

```dotenv
# ================= LLM（沿用源项目 .env） =================
LLM_MODEL=qwen-plus
LLM_BASE_URL=https://ws-bv25nkss8p8gzy9v.cn-beijing.maas.aliyuncs.com/compatible-mode/v1
LLM_API_KEY=<密钥仅存于配置文件，此处以配置中心实际值为准>

# ================= 客服服务 =================
APP_HOST=0.0.0.0
APP_PORT=18082
SESSION_IDLE_TIMEOUT_MINUTES=30        # 会话空闲过期时间（过期后新建 session）

# ================= 金融业务中台（替代 COMMERCE_API_BASE_URL） =================
FINANCE_API_BASE_URL=http://127.0.0.1:8000    # finance-data 服务地址（默认值来自 finance-data/.env 的 APP_PORT）
FINANCE_API_CHANNEL_CODE=AI_CS                  # 调用中台的渠道头（5.4.3 扩展种子）
FINANCE_API_TIMEOUT_CONNECT=5                   # 连接超时（秒）
FINANCE_API_TIMEOUT_READ=30                     # 读取超时（秒）
FINANCE_API_MAX_RETRIES=2                       # 读接口重试次数（写接口不重试）

# ================= 客服对话状态库（重建，连接参数以配置文件为准） =================
CS_DATABASE_URL=mysql+aiomysql://root:123321@127.0.0.1:3306/customer_service?charset=utf8mb4
CS_DB_ECHO=false                                # 是否打印 SQL（调试用）

# ================= 金融业务库（供 finance-data 使用，客服后端不直连） =================
# 以下参数与 finance-data/.env 保持一致：
# DB_HOST=127.0.0.1  DB_PORT=3306  DB_USER=root  DB_PASSWORD=<见配置文件>  DB_NAME=finance
```

#### 5.3.2 finance-data `.env`（保持现有约定）

```dotenv
DB_HOST=127.0.0.1      # 数据库地址
DB_PORT=3306           # 数据库端口
DB_USER=root           # 数据库用户名
DB_PASSWORD=<见配置文件> # 数据库密码（以交付的配置文件实际值为准）
DB_NAME=finance        # 数据库名
APP_PORT=8000          # 应用端口
```

#### 5.3.3 `config/settings.py` 增量

```python
class Settings(BaseSettings):
    # LLM / 应用（沿用）
    llm_model: str
    llm_base_url: str
    llm_api_key: str
    app_host: str
    app_port: int
    # 金融中台（新增）
    finance_api_base_url: str
    finance_api_channel_code: str = "AI_CS"
    finance_api_timeout_connect: int = 5
    finance_api_timeout_read: int = 30
    finance_api_max_retries: int = 2
    # 客服库（更名）
    cs_database_url: str
    cs_db_echo: bool = False
    # 会话治理（新增）
    session_idle_timeout_minutes: int = 30
```

### 5.4 数据库设计

数据库分两类，连接参数均以配置文件为准：

1. **`finance` 业务库**：由 finance-data 的 `init_db.py` 执行 `sql/finance.sql` 一次性建 92 张表，再由数据生成器灌入样本数据；客服系统仅通过 REST API 访问。
2. **`customer_service` 客服库**：本项目 **重新建表**（原 `dialogue_states` 单表方案升级为企业级 6 表方案），由 `scripts/init_cs_db.py` 执行 `sql/customer_service.sql`。

#### 5.4.1 业务库初始化（finance）

```bash
# 在 finance-data 目录下（uv 环境）
uv run init_db.py            # 建库建表（若库已存在则先 DROP 再重建）
uv run -m generate.main --profile smoke   # 小规模样本数据（联调用）
uv run -m generate.main --profile full    # 全量样本数据（演示/验收用）
```

注意事项：

- `init_db.py` 会 **删除并重建** `finance` 库，生产/共享环境执行前必须确认配置文件中 `DB_NAME` 指向正确实例；
- 生成器遵循 README 的九层强约束（主体一致性、金额口径、状态机合法），生成后自动执行验收查询；
- 客服联调建议先用 `smoke`，验证「客户→账户→交易→授信→贷款→工单」链路均有可命中样本后，再切 `full`。

#### 5.4.2 客服库重建（customer_service，全量 DDL）

```sql
-- =====================================================================
-- 客服库 customer_service 重建脚本（sql/customer_service.sql）
-- 字符集 utf8mb4 / 存储引擎 InnoDB；连接参数以 .env 的 CS_DATABASE_URL 为准
-- =====================================================================

CREATE DATABASE IF NOT EXISTS customer_service CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
USE customer_service;

-- 1) 对话状态表（重建自原 dialogue_states：新增客户号绑定、乐观锁、时间戳）
CREATE TABLE IF NOT EXISTS cs_dialogue_state (
    sender_id       VARCHAR(64)  NOT NULL COMMENT '会话用户标识 = 金融客户号 customer_no',
    customer_no     VARCHAR(64)  NOT NULL COMMENT '金融客户号（与 sender_id 同值，显式冗余便于审计）',
    state_json      LONGTEXT     NOT NULL COMMENT 'DialogueState 全量 JSON（任务栈/会话/轮次/卡片）',
    state_version   BIGINT       NOT NULL DEFAULT 0 COMMENT '乐观锁版本号，每次保存 +1',
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (sender_id),
    KEY idx_cds_customer_no (customer_no)
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4 COMMENT = '对话状态持久化表';

-- 2) 消息流水表（历史消息查询、审计回溯；与状态 JSON 双写，查询不再依赖大 JSON 解析）
CREATE TABLE IF NOT EXISTS cs_chat_message (
    id              BIGINT       NOT NULL AUTO_INCREMENT,
    sender_id       VARCHAR(64)  NOT NULL COMMENT '客户号',
    session_id      VARCHAR(64)  NOT NULL COMMENT '会话 ID',
    turn_id         VARCHAR(64)  NOT NULL COMMENT '轮次 ID',
    message_id      VARCHAR(64)  NOT NULL COMMENT '消息 ID',
    role            VARCHAR(16)  NOT NULL COMMENT 'user / bot',
    msg_type        VARCHAR(16)  NOT NULL DEFAULT 'text' COMMENT 'text / object',
    text            TEXT         NULL COMMENT '文本内容',
    object_json     TEXT         NULL COMMENT '业务对象卡片 JSON（id/title/type/attributes）',
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    UNIQUE KEY uk_message_id (message_id),
    KEY idx_ccm_sender_session (sender_id, session_id, id)
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4 COMMENT = '聊天消息流水表';

-- 3) 轮次追踪表（可观测性：意图识别/规划/澄清/耗时全记录）
CREATE TABLE IF NOT EXISTS cs_turn_trace (
    id              BIGINT       NOT NULL AUTO_INCREMENT,
    sender_id       VARCHAR(64)  NOT NULL,
    session_id      VARCHAR(64)  NOT NULL,
    message_id      VARCHAR(64)  NOT NULL COMMENT '触发轮次的用户消息 ID',
    request_id      VARCHAR(64)  NULL COMMENT '链路追踪号（透传中台 X-Request-Id）',
    tracks          VARCHAR(64)  NULL COMMENT '命中轨道：task,knowledge,chitchat',
    plan_json       TEXT         NULL COMMENT 'TurnPlan 原始输出',
    clarify_reason  VARCHAR(64)  NULL COMMENT '澄清原因（ClarifyReason 枚举）',
    flow_id         VARCHAR(64)  NULL COMMENT '轮次结束时的激活流程',
    step_id         VARCHAR(64)  NULL COMMENT '轮次结束时的流程步骤',
    status          VARCHAR(16)  NOT NULL DEFAULT 'ok' COMMENT 'ok / error / degraded',
    error_message   VARCHAR(512) NULL,
    cost_ms         INT          NULL COMMENT '本轮总耗时',
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY idx_ctt_sender (sender_id, id),
    KEY idx_ctt_message (message_id)
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4 COMMENT = '对话轮次追踪表（可观测性）';

-- 4) 工具调用审计表（可观测性 + 金融合规审计：Action 调中台的入参出参全留痕）
CREATE TABLE IF NOT EXISTS cs_action_audit (
    id              BIGINT       NOT NULL AUTO_INCREMENT,
    sender_id       VARCHAR(64)  NOT NULL,
    session_id      VARCHAR(64)  NULL,
    action_name     VARCHAR(64)  NOT NULL COMMENT 'Action 名（如 action_submit_loan_application）',
    method          VARCHAR(8)   NULL COMMENT 'GET / POST / ...',
    url             VARCHAR(256) NULL COMMENT '中台请求路径（不含敏感参数）',
    request_json    TEXT         NULL COMMENT '请求体（敏感字段脱敏后）',
    response_json   TEXT         NULL COMMENT '响应体（截断至 8KB）',
    http_status     INT          NULL,
    biz_code        VARCHAR(64)  NULL COMMENT '中台响应 code（0 或错误码）',
    cost_ms         INT          NULL,
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY idx_caa_sender (sender_id, id),
    KEY idx_caa_action (action_name, created_at)
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4 COMMENT = 'Action 调用审计表';

-- 5) FAQ 表（知识轨 faq.default 的数据源）
CREATE TABLE IF NOT EXISTS cs_faq (
    id              BIGINT       NOT NULL AUTO_INCREMENT,
    category        VARCHAR(64)  NOT NULL COMMENT '分类：rate/fee/repayment/credit_card/risk/account/other',
    question        VARCHAR(256) NOT NULL COMMENT '标准问',
    answer          TEXT         NOT NULL COMMENT '标准答',
    keywords        VARCHAR(512) NULL COMMENT '检索关键词，逗号分隔',
    sort_no         INT          NOT NULL DEFAULT 1,
    status          VARCHAR(16)  NOT NULL DEFAULT 'active' COMMENT 'active / disabled',
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY idx_faq_category (category, status)
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4 COMMENT = 'FAQ 知识表';

-- 6) 知识库文档表（知识轨 rag.default 的数据源；一期全文检索，预留向量字段）
CREATE TABLE IF NOT EXISTS cs_knowledge_doc (
    id              BIGINT       NOT NULL AUTO_INCREMENT,
    category        VARCHAR(64)  NOT NULL COMMENT '分类：product/guide/policy/faq_ext',
    title           VARCHAR(256) NOT NULL,
    content         MEDIUMTEXT   NOT NULL,
    keywords        VARCHAR(512) NULL,
    embedding_json  TEXT         NULL COMMENT '预留：向量嵌入（升级向量检索时使用）',
    status          VARCHAR(16)  NOT NULL DEFAULT 'active',
    created_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME     NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (id),
    KEY idx_kd_category (category, status)
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4 COMMENT = '知识库文档表';
```

> 兼容策略：原 `dialogue_states` 表直接废弃（客服对话状态无跨版本迁移价值，重建即可）；上线切换时先执行本脚本，再发布新版后端。

#### 5.4.3 业务库扩展表（finance 库，向后兼容增量）

**一期（必须）**——智能客服渠道：

```sql
-- sql/ext/001_channel_ai_cs.sql
INSERT INTO dim_channel (channel_code, channel_name, channel_type, channel_status, yn, created_at, updated_at)
SELECT 'AI_CS', '智能客服', 'ai_cs', 'active', 1, NOW(), NOW()
WHERE NOT EXISTS (SELECT 1 FROM dim_channel WHERE channel_code = 'AI_CS');
```

**二期（信用卡/存款/基金域完善，可按业务排期启用）**：

```sql
-- sql/ext/002_credit_card.sql
CREATE TABLE IF NOT EXISTS credit_card_product (
    id                    BIGINT PRIMARY KEY AUTO_INCREMENT,
    product_code          VARCHAR(64)  NOT NULL,
    product_name          VARCHAR(128) NOT NULL,
    card_level            VARCHAR(64)  NOT NULL COMMENT 'standard/gold/platinum/diamond',
    currency_code         VARCHAR(64)  NOT NULL,
    min_credit_limit      DECIMAL(18,2) NOT NULL DEFAULT 0,
    max_credit_limit      DECIMAL(18,2) NOT NULL DEFAULT 0,
    annual_fee_amount     DECIMAL(18,2) NOT NULL DEFAULT 0 COMMENT '年费',
    annual_fee_waive_rule VARCHAR(256) NULL COMMENT '年费减免规则',
    benefits              TEXT         NULL COMMENT '权益说明（JSON 数组）',
    product_status        VARCHAR(64)  NOT NULL DEFAULT 'active',
    created_at            DATETIME NOT NULL,
    updated_at            DATETIME NOT NULL,
    UNIQUE KEY uk_ccp_code (product_code)
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4 COMMENT = '信用卡产品表（扩展）';

CREATE TABLE IF NOT EXISTS credit_card (
    id                    BIGINT PRIMARY KEY AUTO_INCREMENT,
    card_no               VARCHAR(64)  NOT NULL,
    customer_id           BIGINT       NOT NULL,
    product_id            BIGINT       NOT NULL,
    account_id            BIGINT       NULL COMMENT '绑定还款账户',
    credit_limit_amount   DECIMAL(18,2) NOT NULL DEFAULT 0 COMMENT '信用额度',
    used_amount           DECIMAL(18,2) NOT NULL DEFAULT 0 COMMENT '已用额度',
    available_amount      DECIMAL(18,2) NOT NULL DEFAULT 0 COMMENT '可用额度',
    card_status           VARCHAR(64)  NOT NULL DEFAULT 'active' COMMENT 'active/frozen/lost/expired/cancelled',
    loss_reason           VARCHAR(256) NULL COMMENT '挂失原因',
    loss_reported_at      DATETIME     NULL COMMENT '挂失时间',
    bill_day              INT          NULL COMMENT '账单日',
    repayment_day         INT          NULL COMMENT '还款日',
    issued_at             DATETIME NOT NULL,
    expired_at            DATETIME NOT NULL,
    created_at            DATETIME NOT NULL,
    updated_at            DATETIME NOT NULL,
    UNIQUE KEY uk_cc_no (card_no),
    KEY idx_cc_customer (customer_id),
    CONSTRAINT fk_cc_customer FOREIGN KEY (customer_id) REFERENCES customer (id),
    CONSTRAINT fk_cc_product FOREIGN KEY (product_id) REFERENCES credit_card_product (id)
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4 COMMENT = '信用卡表（扩展：额度/挂失/账期）';

-- sql/ext/003_deposit_fund.sql
CREATE TABLE IF NOT EXISTS deposit_product (
    id                 BIGINT PRIMARY KEY AUTO_INCREMENT,
    product_code       VARCHAR(64)  NOT NULL,
    product_name       VARCHAR(128) NOT NULL,
    deposit_type       VARCHAR(64)  NOT NULL COMMENT 'demand 活期 / fixed 定期 / notice 通知 / structured 结构性',
    currency_code      VARCHAR(64)  NOT NULL,
    min_deposit_amount DECIMAL(18,2) NOT NULL DEFAULT 0 COMMENT '起存金额',
    term_months        INT          NULL COMMENT '存期（月），活期为空',
    annual_rate        DECIMAL(12,6) NOT NULL DEFAULT 0 COMMENT '年利率',
    product_status     VARCHAR(64)  NOT NULL DEFAULT 'active',
    created_at         DATETIME NOT NULL,
    updated_at         DATETIME NOT NULL,
    UNIQUE KEY uk_dp_code (product_code)
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4 COMMENT = '存款产品表（扩展）';

CREATE TABLE IF NOT EXISTS fund_product (
    id                 BIGINT PRIMARY KEY AUTO_INCREMENT,
    fund_code          VARCHAR(64)  NOT NULL,
    fund_name          VARCHAR(128) NOT NULL,
    fund_type          VARCHAR(64)  NOT NULL COMMENT 'money/bond/equity/mixed/index',
    risk_level_id      BIGINT       NOT NULL,
    currency_code      VARCHAR(64)  NOT NULL,
    latest_nav         DECIMAL(18,6) NOT NULL DEFAULT 0 COMMENT '最新净值',
    nav_date           DATE         NULL COMMENT '净值日期',
    min_purchase_amount DECIMAL(18,2) NOT NULL DEFAULT 0 COMMENT '起购金额',
    fund_status        VARCHAR(64)  NOT NULL DEFAULT 'selling',
    created_at         DATETIME NOT NULL,
    updated_at         DATETIME NOT NULL,
    UNIQUE KEY uk_fp_code (fund_code),
    CONSTRAINT fk_fp_risk FOREIGN KEY (risk_level_id) REFERENCES dim_risk_level (id)
) ENGINE = InnoDB DEFAULT CHARSET = utf8mb4 COMMENT = '基金产品表（扩展）';
```

**配套扩展接口（finance-data 新增 `app/routers/cards.py`，一期最小集）**：

| 接口 | 说明 |
|---|---|
| `GET /api/v1/cards/{card_no}` | 按卡号查卡与绑定账户/客户（客服「卡号→账户号」解析依据）；校验登录客户归属 |
| `POST /api/v1/cards/{card_no}/loss-report` | 挂失：入参 `request_no`、`loss_reason`；仅 `active` 卡可挂失，置 `card_status=lost`，写审计；幂等返回首次结果。二期兼容 `credit_card` 表（先查 `credit_card`，未命中查 `bank_card`） |

#### 5.5 YAML 流程重构（全量交付物）

> 重构原则：① 步骤/跳转/响应模式语法与源引擎 100% 兼容，引擎零改动；② 所有话术改为金融场景并符合银行客服语气规范（专业、克制、风险提示到位）；③ 每个写操作前必须有完整槽位与确认语义；④ 所有可失败节点显式定义失败分支（查询失败/提交失败 → 引导或转人工）。

##### 5.5.1 `flow_config/system_flows.yml`（金融版，全量）

```yaml
flows:
  system_task_started:
    description: Flow for acknowledging that a new task has started
    name: task started acknowledgement
    steps:
      - id: start
        type: start
        next: acknowledge
      - id: acknowledge
        type: action
        action: action_response
        args:
          mode: static
          text: "好的，我们来办理{{ context.started_flow_name }}。"
        next: end
      - id: end
        type: end
        next: []

  system_collect_information:
    description: Flow for asking the user for a slot value during a collect step
    name: collect information
    steps:
      - id: start
        type: start
        next: ask
      - id: ask
        type: action
        action: action_response
        args: context.response
        next: listen
      - id: listen
        type: action
        action: action_listen
        next: end
      - id: end
        type: end
        next: []

  system_task_resumed:
    description: Flow for acknowledging that a paused task has been resumed
    name: task resumed acknowledgement
    steps:
      - id: start
        type: start
        next: acknowledge
      - id: acknowledge
        type: action
        action: action_response
        args:
          mode: static
          text: "好的，我们继续刚才的{{ context.resumed_flow_name }}。"
        next: end
      - id: end
        type: end
        next: []

  system_task_resume_failed:
    description: Flow for explaining that no paused task can be resumed
    name: task resume failed acknowledgement
    steps:
      - id: start
        type: start
        next: acknowledge
      - id: acknowledge
        type: action
        action: action_response
        args:
          text: "当前没有找到可以继续办理的业务。您可以告诉我需要办理什么业务，例如查余额、查交易、申请贷款或提交投诉。"
        next: end
      - id: end
        type: end
        next: []

  system_task_interrupted:
    description: Flow for acknowledging that the current task has been interrupted
    name: task interrupted acknowledgement
    steps:
      - id: start
        type: start
        next: acknowledge
      - id: acknowledge
        type: action
        action: action_response
        args:
          mode: static
          text: "好的，先把{{ context.interrupted_flow_name }}放一放，我们优先处理{{ context.started_flow_name }}。"
        next: end
      - id: end
        type: end
        next: []

  system_task_canceled:
    description: Flow for acknowledging that the current task was canceled
    name: task canceled acknowledgement
    steps:
      - id: start
        type: start
        next: acknowledge
      - id: acknowledge
        type: action
        action: action_response
        args:
          mode: static
          text: "好的，已为您取消{{ context.canceled_flow_name }}。已收集的信息不会用于其他业务。"
        next: end
      - id: end
        type: end
        next: []

  system_cannot_handle:
    description: Flow for handling requests the assistant cannot support
    name: cannot handle request
    steps:
      - id: start
        type: start
        next:
          - if: "context.get('reason') == 'clarification_rejected'"
            then: clarification_rejected
          - if: "context.get('reason') == 'not_supported'"
            then: not_supported
          - if: "context.get('reason') == 'no_relevant_answer'"
            then: no_relevant_answer
          - else: ask_rephrase

      - id: clarification_rejected
        type: action
        action: action_response
        args:
          mode: rephrase
          text: "抱歉，我刚才可能理解有误。您可以重新说明需要办理的业务，例如查询账户余额、查询交易明细、申请贷款、信用卡挂失或提交投诉工单。"
          prompt: |
            你是一个中文金融客服助手，语气专业、友好、简洁，不做任何收益承诺。
            请基于下面的建议回复，生成一句更自然的中文回复，保持原意，不要扩写。
            对话上下文：
            {history}
            用户最后一句：
            用户：{user_message}
            建议回复：{current_response}
            改写后的回复：
        next: end

      - id: not_supported
        type: action
        action: action_response
        args:
          mode: rephrase
          text: "我理解您的需求，不过该业务暂未接入智能客服。您可以尝试其他业务咨询，或回复“转人工”由人工客服为您处理。"
          prompt: |
            你是一个中文金融客服助手，语气专业、友好、简洁。
            请基于下面的建议回复，生成一句更自然的中文回复，保持原意，不要扩写。
            对话上下文：
            {history}
            用户最后一句：
            用户：{user_message}
            建议回复：{current_response}
            改写后的回复：
        next: end

      - id: no_relevant_answer
        type: action
        action: action_response
        args:
          mode: rephrase
          text: "抱歉，这个问题我暂时没有找到可靠答案。为避免提供不准确的金融信息，建议您换一种方式描述，或回复“转人工”咨询人工客服。"
          prompt: |
            你是一个中文金融客服助手，语气专业、友好、简洁。
            请基于下面的建议回复，生成一句更自然的中文回复，保持原意，不要扩写。
            对话上下文：
            {history}
            用户最后一句：
            用户：{user_message}
            建议回复：{current_response}
            改写后的回复：
        next: end

      - id: ask_rephrase
        type: action
        action: action_response
        args:
          mode: rephrase
          text: "不好意思，我没有完全理解您的意思。您可以直接说明要办理的业务，例如查余额、查交易、申请贷款、信用卡挂失或提交投诉。"
          prompt: |
            你是一个中文金融客服助手，语气专业、友好、简洁。
            请基于下面的建议回复，生成一句更自然的中文回复，保持原意，不要扩写。
            对话上下文：
            {history}
            用户最后一句：
            用户：{user_message}
            建议回复：{current_response}
            改写后的回复：
        next: end

      - id: end
        type: end
        next: []
```

##### 5.5.2 `flow_config/user_flows.yml`（金融版，全量）

```yaml
slots:
  account_no:
    type: text
    label: 账户号
    description: 银行账户号（如 6222 开头的账号，非卡号时由系统解析）

  card_no:
    type: text
    label: 卡号
    description: 银行卡号或信用卡号（16-19 位数字）

  query_date:
    type: text
    label: 查询日期
    description: 交易查询的目标日期（YYYY-MM-DD）或“近 7 天”等范围表述，由系统解析为时间区间

  transaction_no:
    type: text
    label: 交易流水号
    description: 账户交易的流水编号

  loan_type:
    type: text
    label: 贷款类型
    description: 贷款类型，如消费贷款、经营贷款、分期贷款、抵押贷款

  loan_amount:
    type: number
    label: 贷款金额
    description: 申请贷款的金额（元）

  loan_term_months:
    type: number
    label: 贷款期限
    description: 申请贷款的期限（月）

  loan_purpose:
    type: text
    label: 贷款用途
    description: 贷款资金用途，如日常消费、装修、教育、经营周转

  limit_no:
    type: text
    label: 授信额度编号
    description: 客户有效授信额度编号（由系统查询填充，无需向用户索要）

  application_no:
    type: text
    label: 贷款申请编号
    description: 贷款申请提交成功后返回的申请编号（由系统填充）

  loss_card_no:
    type: text
    label: 挂失卡号
    description: 需要挂失的银行卡或信用卡卡号

  loss_reason:
    type: text
    label: 挂失原因
    description: 挂失原因，如丢失、被盗、疑似盗刷

  id_verify_info:
    type: text
    label: 身份验证信息
    description: 用于身份核验的信息（证件号后 6 位或预留手机号），提交中台前需脱敏处理

  ticket_type:
    type: text
    label: 工单类型
    description: 工单类型，取值：账户问题、交易问题、理财问题、贷款问题、还款问题、投诉、其他

  related_business_no:
    type: text
    label: 关联业务编号
    description: 工单关联的业务编号（交易流水号/申请编号等），无则填“无”

  problem_description:
    type: text
    label: 问题描述
    description: 工单的问题描述内容

flows:
  onboarding:
    name: 欢迎引导
    description: 聊天窗口初次打开时欢迎客户，并介绍助手可办理的金融业务。通常由系统主动触发。
    steps:
      - id: start
        type: start
        next: respond
      - id: respond
        type: action
        action: action_response
        args:
          text: "您好，这里是金融智能客服助手。我可以帮您查询账户余额与交易明细，咨询存款、贷款、理财、信用卡等产品信息，办理贷款申请、信用卡挂失，或为您提交投诉工单。请问有什么可以帮您？"
        next: end
      - id: end
        type: end
        next: []

  account_balance_query:
    name: 账户余额查询
    description: 帮客户查询银行账户余额、可用余额与冻结金额。收集账户号或卡号后调用账户查询接口。
    steps:
      - id: start
        type: start
        next: ask_account
      - id: ask_account
        type: collect
        slot_name: account_no
        response:
          text: "请提供您的账户号或银行卡号，我来为您查询账户余额。"
        next: lookup_balance
      - id: lookup_balance
        type: action
        action: action_query_account_balance
        next:
          - if: "slots.get('account_query_success') == 'true'"
            then: show_balance
          - else: lookup_failed
      - id: show_balance
        type: action
        action: action_response
        args:
          mode: static
          text: "您的账户{{ slots.account_no }}当前状态为{{ slots.account_status_desc }}：账户余额 {{ slots.balance_amount }} 元，可用余额 {{ slots.available_amount }} 元，冻结金额 {{ slots.frozen_amount }} 元。{{ slots.account_product_name }}"
        next: end
      - id: lookup_failed
        type: action
        action: action_response
        args:
          mode: rephrase
          text: "{{ slots.account_query_message }}"
          prompt: |
            你是一个中文金融客服助手，语气专业、友好、简洁。
            请基于下面的建议回复，生成一句更自然的中文回复，保持原意，不要扩写。
            对话上下文：
            {history}
            用户最后一句：
            用户：{user_message}
            建议回复：{current_response}
            改写后的回复：
        next: end
      - id: end
        type: end
        next: []

  transaction_query:
    name: 交易明细查询
    description: 帮客户查询账户交易流水。收集账户号或卡号，可选收集查询日期或时间范围，返回交易金额、时间、交易对象等信息。
    steps:
      - id: start
        type: start
        next: ask_account
      - id: ask_account
        type: collect
        slot_name: account_no
        response:
          text: "请提供您的账户号或银行卡号。"
        next: ask_date
      - id: ask_date
        type: collect
        slot_name: query_date
        response:
          text: "请问您想查询哪一天的交易？可以说具体日期（例如 2026-08-26），也可以说“近 7 天”或“最近一个月”。"
        next: lookup_transactions
      - id: lookup_transactions
        type: action
        action: action_query_transactions
        next:
          - if: "slots.get('transaction_query_success') == 'true'"
            then: show_transactions
          - else: lookup_failed
      - id: show_transactions
        type: action
        action: action_response
        args:
          mode: rephrase
          text: "已为您查询到账户{{ slots.account_no }}在{{ slots.query_date_desc }}的交易，共 {{ slots.transaction_count }} 笔：{{ slots.transaction_summary }}如需查看某笔交易的详情，请告诉我交易流水号。"
          prompt: |
            你是一个中文金融客服助手，语气专业、友好、简洁。
            下面是交易查询结果，请整理成清晰的中文回复（可用分号或换行分列各笔交易），
            金额保留两位小数并标注收支方向，不要编造任何数据：
            对话上下文：
            {history}
            用户最后一句：
            用户：{user_message}
            查询结果：{current_response}
            整理后的回复：
        next: end
      - id: lookup_failed
        type: action
        action: action_response
        args:
          mode: rephrase
          text: "{{ slots.transaction_query_message }}"
          prompt: |
            你是一个中文金融客服助手，语气专业、友好、简洁。
            请基于下面的建议回复，生成一句更自然的中文回复，保持原意，不要扩写。
            对话上下文：
            {history}
            用户最后一句：
            用户：{user_message}
            建议回复：{current_response}
            改写后的回复：
        next: end
      - id: end
        type: end
        next: []

  loan_application:
    name: 贷款申请
    description: 引导客户提交消费贷款等贷款申请。依次收集贷款类型、金额、期限、用途，校验授信额度后提交申请并返回申请编号。
    steps:
      - id: start
        type: start
        next: ask_loan_type
      - id: ask_loan_type
        type: collect
        slot_name: loan_type
        response:
          text: "请问您想申请哪种贷款？目前支持消费贷款、经营贷款、分期贷款等，您可以直接说用途，我来帮您匹配。"
        next: ask_amount
      - id: ask_amount
        type: collect
        slot_name: loan_amount
        response:
          text: "请问您计划申请多少金额？（单位：元）"
        next: ask_term
      - id: ask_term
        type: collect
        slot_name: loan_term_months
        response:
          text: "请问贷款期限是多少个月？"
        next: ask_purpose
      - id: ask_purpose
        type: collect
        slot_name: loan_purpose
        response:
          text: "请简单说明贷款用途，例如日常消费、装修、教育或经营周转。"
        next: fetch_credit_limit
      - id: fetch_credit_limit
        type: action
        action: action_fetch_credit_limit
        next:
          - if: "slots.get('limit_no')"
            then: submit_application
          - else: no_credit_limit
      - id: submit_application
        type: action
        action: action_submit_loan_application
        next:
          - if: "slots.get('application_no')"
            then: submit_success
          - else: submit_failed
      - id: submit_success
        type: action
        action: action_response
        args:
          mode: static
          text: "您的贷款申请已提交成功，申请编号为 {{ slots.application_no }}。申请金额 {{ slots.loan_amount }} 元、期限 {{ slots.loan_term_months }} 个月、用途为{{ slots.loan_purpose }}。系统将进行风控审核，审核结果会第一时间通知您。您可以随时问我“贷款申请进度”来查询状态。"
        next: end
      - id: submit_failed
        type: action
        action: action_response
        args:
          mode: rephrase
          text: "{{ slots.loan_submit_message }}"
          prompt: |
            你是一个中文金融客服助手，语气专业、友好、简洁。
            请基于下面的建议回复，生成一句更自然的中文回复，保持原意，不要扩写，不要承诺审批结果。
            对话上下文：
            {history}
            用户最后一句：
            用户：{user_message}
            建议回复：{current_response}
            改写后的回复：
        next: end
      - id: no_credit_limit
        type: action
        action: action_response
        args:
          mode: rephrase
          text: "{{ slots.credit_limit_message }}您也可以回复“转人工”，由客户经理协助您完成授信评估。"
          prompt: |
            你是一个中文金融客服助手，语气专业、友好、简洁，不要承诺额度与审批结果。
            请基于下面的建议回复，生成一句更自然的中文回复，保持原意，不要扩写。
            对话上下文：
            {history}
            用户最后一句：
            用户：{user_message}
            建议回复：{current_response}
            改写后的回复：
        next: end
      - id: end
        type: end
        next: []

  credit_card_loss_report:
    name: 信用卡挂失
    description: 引导客户完成信用卡（或银行卡）挂失。收集挂失卡号、挂失原因并完成身份验证后提交挂失。
    steps:
      - id: start
        type: start
        next: ask_card_no
      - id: ask_card_no
        type: collect
        slot_name: loss_card_no
        response:
          text: "请提供需要挂失的卡号。挂失后该卡将无法交易，请确认后再提供。"
        next: ask_reason
      - id: ask_reason
        type: collect
        slot_name: loss_reason
        response:
          text: "请告诉我挂失原因，例如卡片丢失、被盗或疑似盗刷。"
        next: ask_verify
      - id: ask_verify
        type: collect
        slot_name: id_verify_info
        response:
          text: "为保障您的账户安全，请提供证件号码后 6 位或银行预留手机号进行身份验证。"
        next: report_loss
      - id: report_loss
        type: action
        action: action_report_card_loss
        next:
          - if: "slots.get('loss_result') == 'success'"
            then: report_success
          - else: report_failed
      - id: report_success
        type: action
        action: action_response
        args:
          mode: static
          text: "挂失成功。卡号尾号{{ slots.loss_card_tail }}的卡片已于 {{ slots.loss_time }} 挂失（挂失编号 {{ slots.loss_report_no }}），后续该卡不能发起新交易。请您尽快办理补卡，如存在可疑交易，建议立即提交投诉工单或联系人工客服。"
        next: end
      - id: report_failed
        type: action
        action: action_response
        args:
          mode: rephrase
          text: "{{ slots.loss_message }}"
          prompt: |
            你是一个中文金融客服助手，语气专业、友好、简洁，涉及资金安全时保持谨慎。
            请基于下面的建议回复，生成一句更自然的中文回复，保持原意，不要扩写。
            对话上下文：
            {history}
            用户最后一句：
            用户：{user_message}
            建议回复：{current_response}
            改写后的回复：
        next: end
      - id: end
        type: end
        next: []

  complaint_ticket:
    name: 投诉工单
    description: 为客户创建投诉或问题工单。收集工单类型、关联业务编号与问题描述，提交后返回工单编号及预计处理时间。
    steps:
      - id: start
        type: start
        next: ask_ticket_type
      - id: ask_ticket_type
        type: collect
        slot_name: ticket_type
        response:
          text: "请问您的问题属于哪一类？可选：账户问题、交易问题、理财问题、贷款问题、还款问题、投诉或其他。"
        next: ask_related_no
      - id: ask_related_no
        type: collect
        slot_name: related_business_no
        response:
          text: "如果有关联的业务编号（如交易流水号、贷款申请编号），请告诉我；如果没有，请回复“无”。"
        next: ask_description
      - id: ask_description
        type: collect
        slot_name: problem_description
        response:
          text: "请详细描述您遇到的问题，包括发生时间和具体表现。"
        next: create_ticket
      - id: create_ticket
        type: action
        action: action_create_ticket
        next:
          - if: "slots.get('ticket_no')"
            then: create_success
          - else: create_failed
      - id: create_success
        type: action
        action: action_response
        args:
          mode: static
          text: "工单已创建成功，工单编号 {{ slots.ticket_no }}。我们会在 1-3 个工作日内跟进处理，处理进展会及时通知您。您可以随时凭工单编号查询进度。"
        next: end
      - id: create_failed
        type: action
        action: action_response
        args:
          mode: rephrase
          text: "{{ slots.ticket_message }}"
          prompt: |
            你是一个中文金融客服助手，语气专业、友好、简洁。
            请基于下面的建议回复，生成一句更自然的中文回复，保持原意，不要扩写。
            对话上下文：
            {history}
            用户最后一句：
            用户：{user_message}
            建议回复：{current_response}
            改写后的回复：
        next: end
      - id: end
        type: end
        next: []

  human_handoff:
    name: 人工客服
    description: 将当前对话转交给人工客服处理。
    steps:
      - id: start
        type: start
        next: respond
      - id: respond
        type: action
        action: action_response
        args:
          text: "好的，正在为您转接人工客服，请稍候。人工客服工作时间以网点公示为准，紧急资金安全问题请拨打客服热线。"
        next: end
      - id: end
        type: end
        next: []
```

> **与源项目的语法兼容性核对**：以上 YAML 仅使用源引擎已支持的构造——`start/end/action/collect` 步骤、静态/条件/兜底跳转、`action_response` 的 `static|rephrase` 模式、`{{ slots.* }}` 与 `{{ context.* }}` Jinja2 占位、`slots.get(...)`/`context.get(...)` 条件表达式。`check_result` 这类「空文本分支节点」是源引擎条件边的标准用法，不需要改引擎。

##### 5.6 Action 重构清单与中台接口映射

删除 `task/action/customer/` 下全部电商 Action（`lookup_order_status`、`lookup_logistics`、`recommend_similar_products`），新建 `task/action/finance/` 包。Action 自动发现机制（`builder.py` 的 `pkgutil` 扫描）复用，仅需把扫描包名改为 `financecs.task.action.finance`。

**通用规范**：

- 所有 Action 继承 `Action`，`run(action_kwargs, state) -> ActionResult(messages, updated_slots)`；
- 中台调用统一经 `infrastructure/finance_client.py`（见下），禁止 Action 内自建 httpx 客户端；
- 每次调用自动写 `cs_action_audit`（入参脱敏、出参截断）；
- 失败一律「软失败」：不抛异常中断轮次，而是写入结果槽（`*_message`）走 YAML 失败分支。

###### 5.6.1 金融中台客户端 `infrastructure/finance_client.py`（新增）

```python
class FinanceClient:
    """金融中台统一客户端：鉴权头 / 超时 / 重试 / 幂等 / 审计"""

    def __init__(self, settings): ...

    async def call(self, sender_id: str, method: str, path: str,
                   *, params: dict | None = None, json_body: dict | None = None,
                   idempotent_key: str | None = None) -> FinanceResult:
        """
        请求头（全部来自配置与上下文）：
          Authorization: Bearer {sender_id}          # sender_id 即 customer_no
          X-Channel-Code: {settings.finance_api_channel_code}   # AI_CS
          X-Request-Id: {uuid4}                      # 透传，写入 cs_turn_trace.request_id
        行为：
          - 读接口（GET）：超时 connect={connect}s read={read}s，失败重试 max_retries 次（指数退避 0.5s/1s）
          - 写接口（POST）：不重试；必须携带 json_body["request_no"]（调用方生成）
          - 响应统一解析 {code,message,request_id,data}；code!=0 或 HTTP>=400 → FinanceResult(ok=False, biz_code, message)
        """
```

###### 5.6.2 Action 清单

| Action（name） | 读取槽位 | 中台调用 | 写回槽位 | 失败处理 |
|---|---|---|---|---|
| `action_query_account_balance` | `account_no`（可为卡号） | 卡号形态 → `GET /cards/{card_no}`（扩展）解析账户；`GET /accounts/{account_no}` | `account_query_success`、`account_status_desc`、`balance_amount`、`available_amount`、`frozen_amount`、`account_product_name`、`account_query_message` | 404/403/超时 → `account_query_success=false` + 原因话术（账户不存在/非本人/状态异常） |
| `action_query_transactions` | `account_no`、`query_date` | `GET /accounts/{account_no}/transactions?start_time=&end_time=&page_size=10`（`query_date` 解析：具体日期→当日 00:00-24:00；“近 7 天”“最近一个月”→对应区间；解析失败默认近 7 天） | `transaction_query_success`、`query_date_desc`、`transaction_count`、`transaction_summary`（每笔：时间/类型中文/金额±/对手方，脱敏）、`transaction_query_message` | 同上；空结果返回“未查询到符合条件的交易” |
| `action_fetch_credit_limit` | （`sender_id`） | `GET /customers/{sender_id}/credit-limits` | `limit_no`（取 `limit_status=active` 且可用额度>0 的首条）、`credit_limit_message`（无额度时：说明需先授信评估） | 无额度/接口失败 → `limit_no` 为空，走 `no_credit_limit` 分支 |
| `action_submit_loan_application` | `limit_no`、`loan_amount`、`loan_term_months`、`loan_purpose`、`loan_type` | `POST /loan/applications`，body：`request_no=CS{sender短码}{turn序号}LOAN`、`customer_no`、`limit_no`、`apply_amount`、`apply_term_months`、`repayment_method`（按产品，缺省 `equal_principal_interest`）、`loan_purpose`（映射中台枚举，默认 `consume`） | `application_no`、`loan_submit_message` | 额度不足/期限越界/状态拒绝 → 保留槽位并给出具体原因，引导修改参数或转人工（不重试） |
| `action_report_card_loss` | `loss_card_no`、`loss_reason`、`id_verify_info` | 一期：`POST /cards/{card_no}/loss-report`（扩展接口，body 含 `request_no`、`loss_reason`）；身份验证信息由客服侧与 `GET /customers/{customer_no}` 的实名/预留手机比对（比对规则：证件后 6 位或手机号全等） | `loss_result`（success/failed）、`loss_card_tail`（后 4 位）、`loss_time`、`loss_report_no`、`loss_message` | 卡不存在/非本人/已挂失/已注销 → 对应话术；疑似盗刷关键词命中时追加“建议立即冻结账户并报警”风险提示 |
| `action_create_ticket` | `ticket_type`、`related_business_no`、`problem_description` | `POST /support/tickets`，body：`request_no=CS{sender短码}{turn序号}TICKET`、`customer_no`、`ticket_type`（中文→枚举映射表）、`ticket_title`（由问题描述截断生成）、`ticket_content`、`related_type`/`related_id`（关联号可解析为交易/申请时填充，否则 `none`） | `ticket_no`、`ticket_message` | 失败话术 + 引导重试或转人工 |
| `action_query_loan_application`（扩展） | `application_no` | `GET /loan/applications/{application_no}` | 申请状态中文描述 | 编号不存在 → 澄清 |
| `action_query_ticket`（扩展） | `ticket_no` | `GET /support/tickets/{ticket_no}` | 工单状态、处理结果 | 同上 |

###### 5.6.3 枚举映射表（客服话术 ↔ 中台枚举）

```python
TICKET_TYPE_MAP = {
    "账户问题": "account_issue", "交易问题": "transaction_issue", "理财问题": "wealth_issue",
    "贷款问题": "loan_issue", "还款问题": "repayment_issue", "投诉": "complaint", "其他": "other",
}
TRANSACTION_TYPE_DESC = {
    "transfer": "转账", "consume": "消费", "deposit": "入金", "withdraw": "出金",
    "loan_disbursement": "贷款放款", "loan_repayment": "贷款还款",
    "wealth_purchase": "理财申购", "wealth_redeem": "理财赎回", "wealth_income": "理财收益入账",
    "refund": "退款", "cancel": "撤销", "reversal": "冲正", "adjustment": "调账",
}
ACCOUNT_STATUS_DESC = {"active": "正常", "frozen": "冻结", "restricted": "限制", "closed": "已销户"}
APPLICATION_STATUS_DESC = {
    "created": "已创建", "risk_reviewing": "风控审核中", "manual_reviewing": "人工审批中",
    "approved": "审批通过", "rejected": "审批拒绝", "cancelled": "已取消", "expired": "已过期",
}
```

> 脱敏规则（写回槽位与审计日志时统一执行）：卡号/账户号仅保留后 4 位（展示尾号场景）或按中台返回值原样（查询详情场景由前端控制展示）；证件号只取后 6 位参与比对后即丢弃；手机号展示为 `138****5678`。

##### 5.7 知识轨重构（意图 / 提供者 / FAQ / 知识库）

###### 5.7.1 知识意图表 `knowledge/intents.py`（金融版全量）

```python
KNOWLEDGE_INTENTS: dict[str, KnowledgeIntent] = {
    # —— 实时业务数据类（经金融中台接口） ——
    "account_info": KnowledgeIntent(
        id="account_info", description="账户信息咨询（余额、状态、开户信息）",
        provider_ids=["api.account"], requires_object_type="account"),
    "transaction_info": KnowledgeIntent(
        id="transaction_info", description="交易流水/转账记录咨询",
        provider_ids=["api.transaction"], requires_object_type="transaction"),
    "loan_application_info": KnowledgeIntent(
        id="loan_application_info", description="贷款申请进度咨询",
        provider_ids=["api.loan_application"], requires_object_type="loan_application"),
    "ticket_info": KnowledgeIntent(
        id="ticket_info", description="工单进度咨询",
        provider_ids=["api.ticket"], requires_object_type="ticket"),
    # —— 产品咨询类（经金融中台接口） ——
    "loan_product_info": KnowledgeIntent(
        id="loan_product_info", description="贷款产品咨询（利率、期限、还款方式、准入条件）",
        provider_ids=["api.loan_product"]),
    "wealth_product_info": KnowledgeIntent(
        id="wealth_product_info", description="理财产品咨询（风险等级、收益率、起购金额、开放规则）",
        provider_ids=["api.wealth_product"]),
    "deposit_info": KnowledgeIntent(
        id="deposit_info", description="存款产品与利率咨询",
        provider_ids=["faq.default", "rag.default"]),   # 一期；二期接 api.deposit_product
    "credit_card_info": KnowledgeIntent(
        id="credit_card_info", description="信用卡咨询（年费、权益、额度、还款规则）",
        provider_ids=["faq.default", "rag.default"]),   # 一期；二期接 api.credit_card_product
    "fund_info": KnowledgeIntent(
        id="fund_info", description="基金产品咨询（类型、净值、风险等级）",
        provider_ids=["rag.default"]),                  # 一期；二期接 api.fund_product
    # —— 政策与规则类（FAQ + 知识库） ——
    "rate_policy": KnowledgeIntent(
        id="rate_policy", description="利率说明咨询", provider_ids=["faq.default", "rag.default"]),
    "fee_policy": KnowledgeIntent(
        id="fee_policy", description="手续费规则咨询", provider_ids=["faq.default", "rag.default"]),
    "prepayment_policy": KnowledgeIntent(
        id="prepayment_policy", description="提前还款政策咨询", provider_ids=["faq.default", "rag.default"]),
    "credit_repayment_policy": KnowledgeIntent(
        id="credit_repayment_policy", description="信用卡还款规则咨询", provider_ids=["faq.default"]),
    "risk_notice": KnowledgeIntent(
        id="risk_notice", description="风险提示咨询", provider_ids=["faq.default", "rag.default"]),
    "guide_info": KnowledgeIntent(
        id="guide_info", description="使用指南与办理流程咨询（开户、网银、手机银行等）",
        provider_ids=["rag.default"]),
    "general_finance_info": KnowledgeIntent(
        id="general_finance_info", description="金融通用信息咨询",
        provider_ids=["faq.default", "rag.default"]),
}
```

> `requires_object_type` 与 5.9.3 的卡片类型对齐：用户点击对应卡片且意图需要该对象时，对象属性自动进入 Provider 检索上下文；对象缺失时触发澄清（`MISSING_FOCUSED_OBJECT`，源引擎已支持）。

###### 5.7.2 Provider 重构

| Provider ID | 类型 | 数据来源 | 检索实现 |
|---|---|---|---|
| `api.account` / `api.transaction` / `api.loan_application` / `api.ticket` | 实时接口 | 金融中台详情接口 | 从 `focused_object.attributes` 取业务编号 → 调中台 → 序列化为知识片段 |
| `api.loan_product` | 实时接口 | `GET /loan/products`（+详情） | 按用户表述中的类型词（消费/经营/抵押…）过滤，输出利率区间/期限/还款方式 |
| `api.wealth_product` | 实时接口 | `GET /wealth/products` | 按风险等级词（稳健→P2/P3、进取→P4/P5）过滤，输出收益率/起购/开放规则 |
| `faq.default` | 数据库 | `cs_faq`（status=active） | 关键词命中计分（question+keywords 分词交集），取 Top3；无命中返回空 |
| `rag.default` | 数据库 | `cs_knowledge_doc` | 一期：关键词/标题匹配 Top3；二期：升级 `embedding_json` 向量检索（表已预留字段） |

###### 5.7.3 FAQ 种子（`seeds/cs_faq.csv`，节选）

| category | question | answer（要点） | keywords |
|---|---|---|---|
| rate | 现在贷款利率是多少？ | 消费贷年化利率区间以 `loan_product` 实时数据为准（示例 4.5%-10.8%），最终利率以审批结果为准，不构成承诺 | 贷款利率,消费贷,年化 |
| fee | 转账手续费怎么收？ | 手机银行渠道转账手续费规则（示例：同行免费/跨行按笔）；以 `service_product` 与渠道公示为准 | 转账,手续费,跨行 |
| repayment | 可以提前还款吗？ | 支持提前还款；部分产品收取提前还款违约金，具体看合同约定；还款前请确认账单 | 提前还款,违约金 |
| credit_card | 信用卡怎么还款？ | 支持绑定账户自动扣款/主动还款；请在还款日 24:00 前到账，逾期将影响征信 | 信用卡还款,还款日,最低还款 |
| risk | 理财有风险吗？ | 理财非存款，产品有风险，投资须谨慎；请先完成风险测评，购买与自身风险等级匹配的产品 | 理财,风险,测评 |
| account | 账户被冻结怎么办？ | 冻结可能源于司法、风控或挂失；请携带证件至网点或提交工单，人工核实后处理 | 冻结,解冻,账户 |

知识库种子（`seeds/cs_knowledge_doc.csv`）同步提供：开户指南、手机银行使用指南、贷款办理流程、理财申购适当性规则、投诉处理时限政策等文档。

##### 5.8 提示词模板重构（金融版全量）

`prompt/jinja2/turn_plan.jinja2`——仅替换领域表述，结构不变（`task/knowledge/chitchat` 三轨、命令集、可用 flows 与 intents 注入位点保持一致）：

```
## 任务说明
你的任务是分析当前对话上下文，并生成一个 TurnPlan JSON。

TurnPlan 顶层只允许以下三个字段：
- `task`
- `knowledge`
- `chitchat`

规则：
- 这三个字段的值必须是 JSON 对象或 null。
- 请根据客户真实表达的意图生成结果。
- 如果客户同时表达了多个意图，可以同时填写多个轨道。
- 后续执行引擎一次只能处理一个轨道；如果你输出多个轨道，系统会再向客户澄清先处理哪一个。
- 金融领域特别注意：
  - “查余额/查账户/查交易/查流水”属于 task（账户余额查询/交易明细查询），不是 knowledge。
  - “申请贷款/办贷款”属于 task（贷款申请）；“贷款产品利率/条件咨询”属于 knowledge。
  - “挂失/卡丢了”属于 task（信用卡挂失）；“信用卡年费/权益咨询”属于 knowledge。
  - “投诉/转账没到账要说法”属于 task（投诉工单）。
  - 涉及收益、利率、审批结果时不要在任何字段中做承诺性表述。
- 不要输出额外字段。
- 只输出合法 JSON，不要输出 markdown。
- 最终答案只能是 JSON 文本本身，不要使用 Markdown 代码块包裹。

---

## Task 结构
当客户是在办理业务时，填写 `task`：
（命令集与示例同源模板：start_flow / resume_flow / cancel_flow / set_slots）
可用 flows：
{{ available_flows_json }}

---

## Knowledge 结构
当客户是在咨询信息时，填写 `knowledge`：
允许的 intent（从下列选项中选择，可以选择一个或多个）：
{{ knowledge_intents_json }}

---

## Chitchat 结构
当客户不是咨询信息或者办理业务时，请填写 `chitchat`：
{ "chat": "客户的问题" }

---

## 当前状态
### Active Task
{{ active_task_json }}
### Interrupted Tasks
{{ interrupted_tasks_json }}
### Focused Object
{{ focused_object_json }}

---

## 对话历史
{{ current_conversation }}

---

## 当前任务
请根据客户最后一句话生成 TurnPlan：
"""{{ user_message }}"""

TurnPlan 只允许输出以下形状：
{ "task": null, "knowledge": null, "chitchat": null }

请根据客户真实意图，把对应字段替换成对象；没有命中的字段保持 null。
不要输出解释。不要使用 markdown 代码块。
你的 TurnPlan：
```

`prompt/jinja2/knowledge_respond.jinja2`：

```
你是一个中文金融客服助手，语气专业、友好、简洁。
{% if knowledge_content %}
以下是与用户问题相关的业务数据或知识内容，请优先基于这些内容作答：
{{ knowledge_content }}
{% endif %}
要求：
- 只根据已知信息作答，不要编造产品、利率、费用或政策。
- 涉及利率、收益率、额度、审批结果时，必须说明“以实际审批/产品公示为准”，不做任何承诺。
- 涉及投资类内容必须附带风险提示（理财有风险，投资须谨慎）。
- 如果信息不足，坦诚告知并引导用户提供更多细节或转人工。
- 语气自然，不要机械复述资料原文。
{% if history %}
对话历史：
{{ history }}
{% endif %}
用户当前问题：{{ user_message }}

助手回复：
```

`prompt/jinja2/chitchat_respond.jinja2`：

```
你是一个中文金融客服助手，语气专业、友好、简洁。

请直接回复用户最后一句话。

要求：
- 如果用户是在打招呼，就自然地回一句中文问候。
- 如果用户问你是谁或你叫什么，就说明你是“金融智能客服助手”。
- 如果用户问的是基于最近对话的简单社交问题，也直接自然回答。
- 不要主动推销任何金融产品；如果用户表达了明确的金融诉求，引导其直接说出要办理的业务。
- 不做收益承诺，不提供投资建议。

{% if history %}
对话历史：
{{ history }}
{% endif %}

用户最后一句：{{ user_message }}

助手回复：
```

`prompt/jinja2/clarify_respond.jinja2`：

```
你是一个中文金融客服助手，语气专业、友好、简洁。
你的任务是把一条系统澄清提示改写成更自然的一句话，不要扩写，不要新增信息，不要改变澄清意图。

澄清原因：{{ reason }}
建议回复：{{ clarify_message }}
{% if focused_object %}
当前聚焦对象：{{ focused_object }}
{% endif %}
{% if history %}
对话历史：
{{ history }}
{% endif %}
用户最后一句：{{ user_message }}

改写后的回复：
```

##### 5.9 对话管控与状态模型

###### 5.9.1 状态模型复用与增强

`DialogueState` 结构不变（`active_task` / `paused_tasks` / `active_system_task` / `sessions` / `focused_object` / `pending_turn`），增强点：

| 增强项 | 方案 |
|---|---|
| 客户身份绑定 | 会话创建时校验 `sender_id` 在中台存在（`GET /customers/{sender_id}`，失败则返回开户引导话术）；`customer_no` 同步写入 `cs_dialogue_state.customer_no` |
| 槽位继承策略 | 账户类槽位（`account_no`/`card_no`）在「同一会话内」跨流程共享：新流程启动时，若目标流程缺失的槽位在上一流程已收集且为账户类，则自动预填（可被用户新输入覆盖）。实现位置：`CommandProcessor._start_flow` 中按白名单拷贝 |
| 敏感槽位处理 | `id_verify_info` 仅存于内存态（`active_task.slots`），落库前由 `save_state` 序列化钩子剔除；流程结束/取消时清理 |
| 会话过期 | `Session.activated_at` 距今超过 `SESSION_IDLE_TIMEOUT_MINUTES`（默认 30 分钟）自动开新会话；任务栈随状态保留，跨会话仍可恢复未完成任务 |

###### 5.9.2 流程切换 / 恢复 / 取消（源机制，金融场景示例）

| 场景 | 触发 | 引擎行为 |
|---|---|---|
| 贷款办理中查余额 | 用户：「先帮我查下余额」 | `start_flow(account_balance_query)` → 贷款上下文压入 `paused_tasks` → `system_task_interrupted` 过场白 → 余额查询 → 结束后询问是否继续贷款 |
| 继续原流程 | 用户：「继续办贷款」 | `resume_flow` → 从栈中弹出贷款上下文 → `system_task_resumed` 过场白 → 从挂起步骤继续收集槽位 |
| 恢复不存在的流程 | 用户：「继续刚才的」 | 栈为空 → `system_task_resume_failed` |
| 取消 | 用户：「不办了/取消」 | `cancel_flow` → 清理激活任务上下文 → `system_task_canceled`（敏感槽位一并清理） |

###### 5.9.3 业务对象消息与自动填槽

前端点击卡片发送 `object` 消息，`ChatObject.type` 由电商的 `product/order` 改为金融对象体系：

| `object.type` | 典型来源 | `attributes` 约定 | 自动填槽映射（替换源项目映射表） |
|---|---|---|---|
| `account` | 我的账户卡片 | `account_no`、`account_status`、`balance_amount` | `account_no → account_no` |
| `bank_card` | 我的银行卡/信用卡卡片 | `card_no`(脱敏)、`card_type`、`card_status` | `card_no → account_no`（查询类流程）或 `card_no → loss_card_no`（挂失流程激活时） |
| `transaction` | 交易流水卡片 | `transaction_no`、`transaction_type`、`amount`、`transaction_at` | `transaction_no → transaction_no`（交易详情）或 `→ related_business_no`（工单流程激活时） |
| `loan_product` | 贷款产品卡片 | `product_code`、`rate_range` | `product_code → loan_type`（语义映射，辅助推荐） |
| `wealth_product` | 理财产品卡片 | `product_code`、`risk_level`、`expected_yield_rate` | 进入知识轨检索上下文 |
| `loan_application` / `ticket` | 申请/工单卡片 | 对应业务编号 | `→ application_no` / `→ ticket_no` |

映射逻辑在 `executor.py` 的 collect 自动填槽处实现：当激活流程缺失槽位属于上表目标槽位、且卡片类型匹配时自动写入；否则仅把卡片存入 `focused_object` 供知识轨使用。`ChatHistoryBuilder` 的对象渲染标签同步扩展（「订单/商品」→「账户/卡片/交易/产品/工单」）。

##### 5.10 客服后端接口契约

> 基础路径：`http://{APP_HOST}:{APP_PORT}`（默认 `0.0.0.0:18082`）。所有接口 `application/json; charset=utf-8`（SSE 除外）。

###### 5.10.1 接口总览

| # | 接口 | 方法 | 说明 | 状态 |
|---|---|---|---|---|
| 1 | `/health` | GET | 健康检查（含中台连通性探测） | 新增（替换 `/`、`/test`） |
| 2 | `/api/chat` | POST | 对话（非流式），契约兼容源项目 | 保留+增强 |
| 3 | `/api/chat/stream` | POST | 对话（SSE 流式） | 新增 |
| 4 | `/api/chat/history` | GET | 历史消息（改为读 `cs_chat_message`） | 增强 |
| 5 | `/api/sessions` | POST | 创建新会话（返回 session_id；可选触发 onboarding） | 新增 |
| 6 | `/api/sessions/state` | GET | 查询当前会话状态（激活流程、步骤、槽位、挂起任务） | 新增 |

###### 5.10.2 `POST /api/chat`（非流式）

请求：

```json
{
  "sender_id": "CUS20260000123",
  "text": "帮我查一下账户余额",
  "object": null
}
```

对象消息示例（点击卡片）：

```json
{
  "sender_id": "CUS20260000123",
  "object": {
    "id": "6222021234561234",
    "type": "bank_card",
    "title": "储蓄卡(1234)",
    "attributes": { "card_no": "622202******1234", "card_type": "debit", "card_status": "active" }
  }
}
```

响应：

```json
{
  "message_id": "8f2c…",
  "messages": [
    { "text": "好的，我们来办理账户余额查询。", "object": null },
    { "text": "请提供您的账户号或银行卡号，我来为您查询账户余额。", "object": null }
  ]
}
```

查询成功的响应（对象消息可携带结构化卡片供前端渲染）：

```json
{
  "message_id": "…",
  "messages": [
    {
      "text": "您的账户 6222****1234 当前状态为正常：账户余额 12,580.00 元，可用余额 12,080.00 元，冻结金额 500.00 元。",
      "object": {
        "id": "ACC20260001",
        "type": "account",
        "title": "人民币活期结算账户",
        "attributes": { "balance_amount": "12580.00", "available_amount": "12080.00", "frozen_amount": "500.00", "account_status": "active" }
      }
    }
  ]
}
```

###### 5.10.3 `POST /api/chat/stream`（SSE）

- 请求体与 `/api/chat` 相同；响应 `Content-Type: text/event-stream`；
- 事件协议：

| event | data | 说明 |
|---|---|---|
| `plan` | `{"tracks":["knowledge"]}` | 轮次规划结果（可选订阅，调试用） |
| `message_start` | `{"message_id":"…"}` | 一条机器人消息开始 |
| `delta` | `{"text":"片段"}` | LLM 增量文本（knowledge/chitchat/rephrase 场景） |
| `message_end` | `{"message_id":"…","object":{...}|null}` | 消息结束（对象卡片在 end 事件携带） |
| `done` | `{"request_id":"…","cost_ms":1234}` | 轮次结束 |
| `error` | `{"code":"…","message":"…"}` | 轮次内异常（连接保持，前端按降级话术展示） |

- 实现要点：仅 LLM 生成类回复走流式（`astream`），静态模板回复直接整条 `message_end`；每个 `delta` 之前先完成状态读取，轮次结束时一次性 `save_state` + 写 `cs_turn_trace`；客户端断开（`await request.is_disconnected()`）时中止 LLM 调用并落审计。

###### 5.10.4 `GET /api/chat/history`

`?sender_id=CUS20260000123[&session_id=…]`。响应：

```json
{
  "sender_id": "CUS20260000123",
  "messages": [
    { "session_id": "S-…", "role": "user", "text": "帮我查一下账户余额", "object": null },
    { "session_id": "S-…", "role": "bot", "text": "请提供您的账户号或银行卡号…", "object": null }
  ]
}
```

###### 5.10.5 `POST /api/sessions` 与 `GET /api/sessions/state`

```json
// POST /api/sessions 请求
{ "sender_id": "CUS20260000123", "trigger_onboarding": true }
// 响应
{ "session_id": "S-20260827-0001", "messages": [ { "text": "您好，这里是金融智能客服助手…" } ] }
```

```json
// GET /api/sessions/state?sender_id=CUS20260000123 响应
{
  "sender_id": "CUS20260000123",
  "session_id": "S-20260827-0001",
  "active_task": { "flow_id": "loan_application", "flow_name": "贷款申请", "step_id": "ask_term", "slots": { "loan_type": "消费贷款", "loan_amount": "50000" } },
  "paused_tasks": [ { "flow_id": "account_balance_query", "flow_name": "账户余额查询" } ],
  "focused_object": null
}
```

> 该接口用于前端调试页与坐席辅助视图；生产环境需叠加访问控制（见 5.12）。

##### 5.11 前端改造方案

> 参考工程：`customer-service-frontend`（Vue 3 + Vite 6，单页 `src/App.vue`，含数字人视频组件 `lm-avatar-chat-sdk`，dev 端口 5174，`/api` 代理到 `127.0.0.1:18082`）。参考工程仅作为骨架参考，**内容与风格全面改造**。

###### 5.11.1 风格改造（电商 → 金融）

| 维度 | 参考工程现状 | 目标态 |
|---|---|---|
| 品牌 | “Atguigu 电商助手”、电商橙色调、商品/订单侧栏 | “金融智能客服”、银行级深蓝 + 金色点缀、盾形/金融图标体系 |
| 布局 | 左侧电商商品/订单栏 + 右侧聊天 + 数字人 | 左侧「我的金融面板」（账户卡片、最近交易、我的持仓/贷款）+ 中间聊天 + 顶部客户信息条 |
| 组件风格 | 商品卡片、订单卡片 | 账户卡片（余额/可用/冻结）、交易卡片（收支方向着色）、产品卡片（收益率+风险等级标签+风险提示）、工单/申请状态卡片 |
| 文案 | 电商导购语气 | 银行客服语气；所有收益展示附“业绩比较基准，不代表收益承诺”；投资类内容附风险提示 |
| 数字人 | 欢迎/转接视频 | 可保留占位或替换为静态金融客服形象（按资源情况） |

###### 5.11.2 功能改造要点

1. **登录态**：新增客户号输入/选择（演示环境可从种子客户下拉选择），`sender_id = customer_no`，持久化于 `localStorage`；
2. **会话管理**：进入页面调用 `POST /api/sessions` 获取 session 与欢迎语；「新会话」按钮重新创建；`GET /api/chat/history` 恢复历史；调试抽屉展示 `GET /api/sessions/state`；
3. **消息协议**：`POST /api/chat` 保持；新增 SSE 模式开关（默认开），`EventSource`/`fetch` 流式消费 `/api/chat/stream`，`delta` 逐字渲染、`message_end` 挂卡片；
4. **业务卡片**：`object.type` 渲染组件：`account`/`bank_card`/`transaction`/`loan_product`/`wealth_product`/`loan_application`/`ticket`；点击卡片以对象消息回传（5.9.3 自动填槽）；
5. **左侧金融面板**：通过客服后端透传或直连中台演示接口展示客户账户列表与最近交易（演示环境）；
6. **代理配置**：`vite.config.js` 保留 `/api → http://127.0.0.1:18082`；删除 `/commerce` 电商中台代理；如需前端直连中台，新增 `/finance → {FINANCE_API_BASE_URL}` 代理（仅演示环境）。

###### 5.11.3 前端目录建议

```
finance-cs-frontend/
├── index.html / package.json / vite.config.js
├── public/（logo、形象资源）
└── src/
    ├── main.js / App.vue（布局壳）
    ├── api/            # chat.js（含 SSE）、session.js、financePanel.js
    ├── components/     # MessageBubble / ObjectCards/* / ClarifyBar / DebugDrawer / MyPanel
    ├── stores/         # session 状态（可用 reactive 或 pinia）
    └── styles/         # 金融主题变量（深蓝/金/中性灰）、卡片样式
```

##### 5.12 安全与合规设计

| 领域 | 设计 |
|---|---|
| 身份与越权 | 客服后端以 `sender_id` 作为唯一客户身份；调中台时 Bearer=客户号，中台负责归属校验（客户只能访问本人对象）；客服后端不缓存他人业务数据；状态查询接口仅返回本人状态 |
| 敏感数据 | 卡号/账号展示脱敏（保留后 4 位）；证件号仅比对后 6 位且即时丢弃；`id_verify_info` 不落库；审计日志入参先经脱敏管道；LLM 提示词中注入的业务数据同样脱敏 |
| 密钥管理 | `LLM_API_KEY`、数据库密码仅存 `.env`/密钥管理；`.gitignore` 排除；文档与代码不出现明文密钥 |
| 内容合规 | 提示词硬约束：不做收益承诺、不给出投资建议、投资必附风险提示；贷款类话术不承诺审批结果；疑似盗刷/欺诈关键词触发风险引导话术并建议人工 |
| 审计留痕 | 全量轮次追踪（`cs_turn_trace`）+ 工具调用审计（`cs_action_audit`）+ 中台侧 `X-Request-Id`/渠道 `AI_CS` 双端对账；满足“关键节点日志记录与审计”要求 |
| 幂等与资金安全 | 写接口强制 `request_no`；客服端同一轮次重发不产生重复申请/工单；挂失等敏感操作前流程内显式确认话术 |
| 传输 | 生产环境全链路 HTTPS（本期演示环境允许 HTTP，部署清单中标注） |

##### 5.13 非功能设计

###### 5.13.1 可靠性

- **轮次隔离**：`DialogueEngine.handle_message` 外层捕获异常 → 返回 `system_cannot_handle` 兜底话术，状态回滚到本轮开始前（不保存脏状态）；
- **中台调用治理**：`FinanceClient` 超时（连接 5s/读取 30s）、读接口重试（2 次指数退避）、写接口不重试只回放幂等号；中台 5xx/超时转「系统繁忙」话术并写审计；
- **状态保存降级**：`save_state` 失败 → 记错误日志 + `cs_turn_trace.status=degraded`，响应照常返回（下一轮重新读取最后成功状态）；
- **依赖健康**：`/health` 聚合 LLM 与中台连通性；启动时预热中台 `GET /health`。

###### 5.13.2 可观测性

| 层 | 手段 |
|---|---|
| 请求级 | 每轮生成 `request_id`，贯穿客服日志、中台 `X-Request-Id`、SSE `done` 事件 |
| 意图与规划 | `cs_turn_trace.plan_json` 保存 TurnPlan 原文与命中轨道；澄清原因落 `clarify_reason` |
| 工具调用 | `cs_action_audit` 记录 Action→中台的 URL/入参/出参/耗时/业务码 |
| 流程节点 | 每轮记录结束时的 `flow_id/step_id`，可回放流程路径 |
| 结构化日志 | 统一 `logging` JSON 格式（时间/级别/request_id/sender_id/模块/消息），关键节点（规划前后、Action 前后、保存前后）打点 |

###### 5.13.3 准确性

- 查询结果一律来自中台实时数据，知识回复提示词强制“只依据检索内容”；
- 槽位校验：`loan_amount`/`loan_term_months` 数值化与产品区间校验（中台二次校验兜底）；卡号/账号长度与数字格式在 Action 内预检；
- 意图侧：`turn_plan` 提示词内置金融领域易混淆规则（办理 vs 咨询的判定，见 5.8）；多意图强制澄清，避免误办。

###### 5.13.4 可扩展性

| 扩展场景 | 扩展路径 | 是否改引擎 |
|---|---|---|
| 新增业务流程（如理财申购、转账） | 加 YAML flow + 注册新 Action | 否 |
| 新增知识类型（保险/证券/外汇） | 加意图 + Provider（+扩展表/接口） | 否 |
| 新增中台接口 | `FinanceClient` 增加封装 + Action | 否 |
| FAQ 升级向量检索 | `cs_knowledge_doc.embedding_json` + 向量 Provider 替换 | 否 |
| 多渠道（电话/网银内嵌） | `X-Channel-Code` 与前端适配层 | 否 |

---

## 6. 迁移实施计划

### 6.1 总体策略

- **内核保留、边界重写**：对话引擎内核不动，重写全部「领域边界件」（YAML/Action/意图/提示词/表结构/前端），把回归风险收敛到边界层；
- **数据先行**：先完成 `finance` 库初始化与样本数据，再开发 Action（可直接联调真实接口）；
- **一期/二期切分**：一期交付需求说明全部验收项（缺口用流程话术+最小扩展接口兜底）；二期补齐信用卡/存款/基金实体与向量检索。

### 6.2 阶段任务分解（WBS）

| 阶段 | 任务 | 交付物 | 责任角色 | 预估工作量 |
|---|---|---|---|---|
| P0 准备 | 环境检查（Python 3.12+/uv/MySQL）；两份源码与资源归档；配置文件整理（5.3 模板） | 环境清单、统一 `.env` | 运维/后端 | 0.5 天 |
| P1 数据层 | `init_db.py` 建 `finance` 库；`generate --profile smoke/full` 灌数；执行扩展脚本 001（AI_CS 渠道）；跑通 `tests/` 验收 | 可查询的金融业务库 | 后端 | 1 天 |
| P2 中台扩展（一期最小） | `cards.py` 路由：`GET /cards/{card_no}`、`POST /cards/{card_no}/loss-report`（幂等+归属校验+审计）+ 路由测试 | 扩展接口可用 | 后端 | 1 天 |
| P3 客服库重建 | `sql/customer_service.sql` + `init_cs_db.py` + 种子导入（FAQ/知识） | 客服库 6 表 + 种子 | 后端 | 0.5 天 |
| P4 后端改造 | 主包更名与配置项（5.3）；`FinanceClient`；金融 Action 集（5.6）；YAML 两份（5.5）；知识轨（5.7）；提示词（5.8）；自动填槽映射（5.9.3）；消息流水/审计落库 | 可对话的客服后端 | 后端/算法 | 3-4 天 |
| P5 接口增强 | `/health`、SSE、`/api/sessions`、`/api/sessions/state`、历史接口改造 | 接口契约 5.10 全量 | 后端 | 1 天 |
| P6 前端改造 | 金融风格主题；业务卡片组件；SSE 消费；金融面板；代理配置 | 演示前端 | 前端 | 2-3 天 |
| P7 联调与测试 | 端到端 7 大场景（4.3）；异常与降级用例；性能冒烟 | 测试报告 | 测试 | 1-2 天 |
| P8 验收交付 | 对照第 8 章验收清单演示；文档归档；部署说明 | 验收记录 | 全体 | 0.5 天 |

> 总计约 10-13 人天（不含二期）。关键路径：P1 → P2 → P4 → P7。

### 6.3 风险与应对

| 风险 | 等级 | 应对 |
|---|---|---|
| 贷款申请依赖授信额度，样本客户无有效额度导致流程演示失败 | 高 | 数据生成后预筛「有活跃额度客户」作为演示账号；流程内实现无额度引导分支（已设计） |
| 挂失一期无专用接口 | 高 | P2 最小扩展接口先行；若不可改中台，降级为「工单挂失」话术（创建 `account_issue` 工单并提示人工冻结） |
| LLM 槽位抽取不稳定（金额/期限/卡号） | 中 | `turn_plan` 金融规则约束 + Action 内格式预检 + 失败时 `system_collect_information` 重问；必要时对关键槽位启用 `validated` 校验 |
| 提示词中的金融合规表述遗漏 | 中 | 5.8 模板内置硬约束；测试清单纳入合规断言（见 8.3） |
| 中台幂等冲突（同幂等号换参数 409） | 中 | `request_no` 绑定轮次与动作，参数变更必然换幂等号 |
| 状态大 JSON 膨胀（长会话） | 低 | 会话过期策略 + `sessions` 仅保留最近 N 轮（提示词窗口已是最近 10 轮） |

### 6.4 回滚方案

- 客服库与业务库分离，重建失败不影响业务库；
- 改造在独立分支/目录进行，源电商工程保留不动，任何阶段可切回旧版演示；
- 中台扩展均为增量脚本与新增路由，回滚只需停用扩展路由。

---

## 7. 部署与启动指南

### 7.1 环境要求

| 项 | 要求 |
|---|---|
| Python | >= 3.12（两个后端服务一致），推荐 `uv` 管理依赖 |
| MySQL | 8.x，utf8mb4；地址/端口/账号/密码以配置文件为准 |
| Node | >= 18（前端 dev/build） |
| 网络 | 客服后端 → LLM 网关、客服后端 → 金融中台（内网直连） |

### 7.2 启动顺序（演示环境）

```bash
# ① 金融业务库初始化 + 样本数据（首次或重置时）
cd finance-data
uv sync
uv run init_db.py
uv run -m generate.main --profile smoke      # 联调；正式演示用 --profile full
# （可选）执行扩展脚本：AI_CS 渠道
#   通过任意 MySQL 客户端执行 sql/ext/001_channel_ai_cs.sql

# ② 启动金融业务数据服务（默认 :8000，端口以其 .env 的 APP_PORT 为准）
uv run -m app.main
# 验证：GET /health → {"data":{"status":"ok"}}

# ③ 客服库初始化（首次或重建时）
cd finance-customer-service
uv sync
uv run scripts/init_cs_db.py                 # 执行 sql/customer_service.sql
uv run scripts/import_seeds.py               # 导入 FAQ / 知识库种子

# ④ 启动客服后端（默认 :18082，以 .env 的 APP_PORT 为准）
uv run main.py
# 验证：GET /health；POST /api/chat 冒烟

# ⑤ 启动前端
cd finance-cs-frontend
npm install
npm run dev                                   # http://127.0.0.1:5174
```

### 7.3 配置核对清单（启动前）

- [ ] 客服后端 `.env`：`LLM_*` 可用；`FINANCE_API_BASE_URL` 指向已启动的中台；`CS_DATABASE_URL` 可达；`FINANCE_API_CHANNEL_CODE=AI_CS` 已在业务库生效；
- [ ] finance-data `.env`：`DB_*` 指向目标 MySQL 实例，`DB_NAME=finance`，确认无同名重要库（初始化会先删库）；
- [ ] 前端 `vite.config.js`：`/api` 代理目标为客服后端地址；
- [ ] 演示客户：`generate` 后选取状态为 `active`、有账户/交易/（最好有授信额度）的客户号用于验收。

---

## 8. 测试与验收方案

### 8.1 验收标准对照（需求说明第 8 章）

| # | 验收标准 | 验证用例 | 判定 |
|---|---|---|---|
| 1 | 完成账户、银行卡、贷款、理财、基金、信用卡等业务咨询 | K-01 贷款产品咨询；K-02 稳健理财推荐；K-03 存款利率；K-04 信用卡年费权益；K-05 基金（一期知识库口径） | 回复基于实时数据/知识库且含合规表述 |
| 2 | 多轮持续收集并利用客户身份、卡号、流水号、贷款编号等槽位 | T-01 分三轮补全账户查询槽位；T-02 对象卡片点击自动填槽；T-03 会话恢复后槽位仍在 | 状态接口可见槽位；无重复追问 |
| 3 | 完成贷款申请、信用卡挂失、投诉工单完整任务流程 | T-10 贷款申请全链路（返回申请编号，中台可查）；T-11 挂失（卡状态变 `lost`）；T-12 工单（返回工单编号，中台可查） | 中台数据落库 + 幂等重放返回同一编号 |
| 4 | 完整闭环演示：提问→意图识别→查询/流程→回复→状态更新 | E-01 全程开 `cs_turn_trace` 观察：轨道命中、Action 审计、状态落库 | 链路五环节均有记录 |
| 5 | 状态持久化与会话恢复 | E-02 重启服务后继续未完成的贷款申请；E-03 过期会话自动新开且可恢复挂起任务 | 流程从中断步骤继续 |

### 8.2 功能测试要点（节选）

| 类别 | 用例 | 期望 |
|---|---|---|
| 意图 | 「查余额」=task；「贷款利率多少」=knowledge；「贷款怎么办」=task；多意图同时表达 | 正确分轨；多意图触发澄清 |
| 澄清 | 模糊输入「帮我看看」；对象消息无意图 | `ClarifyResponder` 自然话术澄清 |
| 流程管控 | 贷款中查余额再恢复；取消；恢复不存在的流程 | 过场话术正确、槽位保留/清理正确 |
| 异常 | 查不存在账户；非本人账户；额度不足；中台停机 | 软失败话术、不抛 500、审计有记录 |
| 幂等 | 同一轮重复提交贷款申请 | 返回同一 `application_no` |
| SSE | 知识问答流式输出 | `delta` 连续、`done` 收尾、断连不落脏状态 |
| 合规 | 理财咨询回复 | 含风险提示、无收益承诺（自动断言关键词） |

### 8.3 非功能验证

- 可靠性：中台停止时全部场景返回降级话术且服务不崩溃；`save_state` 故障注入时响应正常返回；
- 可观测性：任一请求可由 `request_id` 在 `cs_turn_trace` + `cs_action_audit` + 日志三处串起；
- 性能冒烟：单实例下非流式对话 P95 < 6s（LLM 为主要耗时），中台读接口重试不放大故障。

---

## 9. 附录

### 9.1 关键枚举速查（客服域高频）

| 枚举 | 取值 |
|---|---|
| `customer_status` | `pending_kyc` 待实名 / `active` 正常 / `restricted` 限制 / `frozen` 冻结 / `closed` 销户 |
| `account_status` | `active` / `frozen` / `restricted` / `closed` |
| `card_status` | `active` / `frozen` / `lost` / `expired` / `cancelled` |
| `transaction_type` | `transfer` / `consume` / `deposit` / `withdraw` / `loan_disbursement` / `loan_repayment` / `wealth_purchase` / `wealth_redeem` / `wealth_income` / `refund` / `cancel` / `reversal` / `adjustment` |
| `transaction_status` | `created` / `processing` / `success` / `failed` / `reversed` |
| `application_status`（贷款） | `created` / `risk_reviewing` / `manual_reviewing` / `approved` / `rejected` / `cancelled` / `expired` |
| `ticket_type` | `account_issue` / `transaction_issue` / `wealth_issue` / `loan_issue` / `repayment_issue` / `complaint` / `other` |
| `ticket_status` | `submitted` / `processing` / `waiting_customer` / `resolved` / `closed` / `rejected` |
| `dim_channel`（常用） | `MOBILE_BANK` / `ONLINE_BANK` / `COUNTER` / `OPEN_API` / `AI_CS`（扩展） |
| 风险等级 | 客户 `C1`保守~`C5`进取；产品 `P1`低~`P5`高 |

### 9.2 中台常见错误码（客服侧需话术化处理）

| 错误码 | 含义 | 客服话术策略 |
|---|---|---|
| `PRINCIPAL_NOT_FOUND` | 登录态客户/员工不存在 | 引导核实客户号 |
| `CUSTOMER_STATUS_FORBIDDEN` | 客户状态不允许办理 | 说明状态并引导人工 |
| `CUSTOMER_SCOPE_FORBIDDEN` | 越权访问他人对象 | 提示只能查询本人业务 |
| `ACCOUNT_NOT_FOUND` / `CARD_NOT_FOUND` | 账户/卡不存在 | 引导核对号码 |
| `CREDIT_LIMIT_NOT_ENOUGH` / `CREDIT_LIMIT_STATUS_FORBIDDEN` | 额度不足/状态异常 | 说明原因，引导调整金额或授信 |
| `LOAN_TERM_OUT_OF_RANGE` | 期限超出产品范围 | 提示产品期限区间，重新收集 |
| `IDEMPOTENCY_PAYLOAD_MISMATCH` | 幂等号复用但参数不一致 | 系统自动更换幂等号重试一次，仍失败转人工 |
| `CHANNEL_NOT_AVAILABLE` | 渠道不可用 | 运维排查项（配置告警） |

### 9.3 术语表

| 术语 | 说明 |
|---|---|
| 三轨规划 | 将每轮输入规划为 task/knowledge/chitchat 三条执行轨道的 LLM 路由机制 |
| 流程（Flow） | YAML 定义的对话步骤图，分业务流程与系统流程 |
| 槽位（Slot） | 任务所需的结构化字段（如账户号、金额） |
| 聚焦对象（FocusedObject） | 用户最近点击的业务卡片 |
| 幂等号（request_no） | 写接口防重复提交标识，中台按 渠道+业务类型+幂等号 记忆首次结果 |
| 适当性 | 理财销售中客户风险等级与产品风险等级匹配要求 |

### 9.4 参考资料路径

| 资料 | 路径 |
|---|---|
| 电商源工程 | `D:\000_项目\0331_ecommerce_customer_service\sz0331-ecommerce-customer-service` |
| 源工程架构拆解 | 同上 `项目拆解\电商智能客服项目拆解.md` |
| 金融业务资源 | `D:\000_AI_大模型_课件\尚硅谷大模型项目实战之电商小二实战\finance-data`（README.md 为数据与接口权威说明） |
| 需求说明 | `D:\000_AI_大模型_课件\尚硅谷大模型项目实战之电商小二实战\1.资料\金融\需求说明.md` |
| 前端参考 | `D:\000_AI_大模型_课件\项目一智能客服\day01_项目介绍&初始化\4_other\中台+前端项目\前端服务\customer-service-frontend` |
| 配置基线 | 源工程 `.env` 与 `finance-data\.env` |

---

> **文档结语**：本方案以「最小改动中台、内核复用、边界重写」为原则，确保从电商到金融的改造在技术上可落地、在业务上可验收。后续迭代（二期）将按 5.4.3 与 6.1 的规划补齐信用卡、存款、基金实体与向量知识检索能力。
